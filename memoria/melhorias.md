# Backlog estratégico — DS + Code Ops (Main Hub)

> Só itens de **Design System / processo / tooling / eficiência**. A execução por projeto (telas, copy, fluxos) vive na pasta do projeto (ex.: `../invoice-flows/memoria/melhorias.md`).

## Ops — separação do repo `design-system` (Storybook) — 2026-08-07
- [ ] **Decidir destino do repo antigo** `paulosilva-designer/EDS---Design-System` (28 commits, conteúdo misto) depois que o repo novo do Storybook estiver publicado e validado — arquivar, deletar ou manter como histórico. Ver `estado-atual.md` (2026-08-07) pro contexto completo.
- [ ] **Mesclar o doc autoral do EdsButton (A10) na MDX viva (A9).** `.claude/references/pendente-doc-a10-edsbutton.mdx` (17 seções, WCAG medido: 5 variantes reprovam contraste AA — success 3.35 / danger 3.11 / success-outline 3.75 / danger-outline 3.48 / link-danger 3.48 — + anel de foco sobre fill sólido 1.39<3.0 + `loading` sem `aria-busy`) ainda não foi mesclado em `src/stories/buttons/EdsButton.mdx` do projeto `design-system`. Era o "próximo passo natural" já apontado em `aprendizados.md` (2026-07-25) — só resgatando aqui pra não se perder depois da limpeza do repo.

## Ops — bot de Slack pro Agente 12 (`../video-bot-slack/`) — 2026-08-13
- [ ] **M93** [Ops][A12] **Fase 0 do plano (`.claude/plans/snug-swinging-engelbart.md`) — pendências antes do 1º teste real.** Código escrito e com sintaxe validada, mas nada testado ao vivo. Falta: (a) Paulo criar o Slack App (Bot Token/Signing Secret) e o namespace novo no Upstash — passo a passo em `cloudflare-worker/README.md`; (b) decidir e aplicar como o clone do `<PRODUTO>` fica read-only no nível do sistema de arquivos na máquina que rodar o worker persistente (hoje só documentado como pendência em `worker-persistente/README.md`, não resolvido); (c) validar/ajustar o allowlist de comandos `Bash` da Fase 3 (`worker-persistente/src/agent.js`, função `gravarVideo`) contra uma gravação real — os padrões escritos são uma primeira tentativa, não testados; (d) confirmar o formato exato da resposta de `convertWithTimestamps` do SDK do ElevenLabs instalado (`audio_base64` vs `audioBase64`) — `worker-persistente/src/index.js` tenta os dois nomes, mas não foi validado contra uma chamada real; (e) gerar o LaunchAgent (plist) pra deixar o worker persistente sempre ligado na máquina do Paulo, hoje só descrito em texto no README.
- [ ] Migrar a chave do ElevenLabs pra conta/billing da própria <EMPRESA> (pendência ativa citada no bot, não bloqueante pro v1 — hoje cobra o cartão pessoal do Paulo, com teto diário de 4 vídeos narrados/dia como proteção temporária).
- [ ] Generalizar `~/bin/<produto>-sync` (hoje tem o path da máquina do Paulo fixo no script) antes de migrar o worker persistente pra uma VM (Fase 2 do plano).

## Design System (Figma × código)
- [ ] **M92** [DS][A4] **Drift Score ao vivo no `EdsButtonIcon` (2026-08-11) — 3 achados reais.** Primeira auditoria completa D1-D6 rodada ao vivo (Figma MCP) contra o componente Button Icon, a pedido do Paulo. (a) **D3 real:** Figma usa `background-brand-pressed` = `#2B2256` (brand-800); o código (`root.scss:53`) resolve `--background-brand-pressed` pra `#403382` (brand-700) — Figma está 1 tom mais escuro que o app de verdade. Ambos passam contraste, não é achado de a11y, é achado de fidelidade Figma×código. (b) **Nomenclatura frágil:** as variáveis do Figma `background-primary-hover`/`-disabled`/`-pressed` não existem no código com esse nome (código usa `--background-hover`/`-disabled`/`-pressed`, sem "primary") — hex coincide hoje (#F2F2F2/#E5E5E5), mas os dois lados podem descolar sem ninguém perceber se alguém editar um dos dois achando que são independentes. (c) **Typo no Figma:** enum de estado grafado "DIsabled" (I maiúsculo) — cosmético, não afeta token/contraste. D1/D5/D6 = limpos (0% hex solto, 0 reprovação de contraste ícone/fundo, 0 papel errado). Detalhe em `aprendizados.md` (2026-08-11).
- [x] **M59** [A11y][DS] **FEITO (2026-07-31) — Border/Hover, token novo.** Nenhum campo de formulário do DS reagia no hover além do fundo (`AthInputThemeDefault.vue`: `&:hover { background: var(--background-hover); }`, nunca a borda) — gap real apontado por Paulo (referência: campos do Google, que escurecem a borda no hover, distinto do foco). Criado `Border/Hover` = neutral-550 `#737373` (4.74:1), reaproveitado da escala primitiva já existente — aplicado a todo campo que usa a classe `.box` no artefato (texto, select, datepickers, upload). Aplicação real no código do <PRODUTO> é decisão/execução futura do time.
- [x] **M62** [A11y][DS] **FEITO (2026-07-31) — auditoria de papel de token (role mismatch) + página de Paleta viva + padronização do anel de foco.** Pedido do Paulo: auditar se algum token que já criei (Text/Background/Border/Overlay) foi aplicado num componente que não é daquele papel (ex.: token de TEXTO usado como FUNDO) — "não tem problema criar token, o problema é token ambíguo". **Achados confirmados e corrigidos:** (a) `EbTippy` e `AthRibbon` ("dark") citavam "Text/Primary" pro FUNDO do tooltip/ribbon — criado token explícito **Background/Inverse** (#002a3a, mesmo hex do Text/Primary, papel diferente). (b) `EdsTable` e `AthInputCheckbox` citavam "Text/Brand" pro preenchimento do checkbox (`accent-color`, não é texto) — criado **Control/Accent** (#6b55d8). (c) `AthLoading` citava "Text/Brand" pro spinner (na verdade `border-color` de um anel) — corrigido pra **Border/Brand** (token que já existia, só mal aplicado). (d) `AthWizard` colapsava 3 papéis (texto do rótulo, borda do círculo, fundo do pontinho) sob 1 rótulo "Text/Brand" — separado em Text/Brand · Border/Brand · Background/Brand. **Nova aba "Paleta de Cores"** no artefato (grupo topo, ao lado de Princípios): lista viva de ~45 tokens em blocos de cor grandes, organizados por categoria (Marca, Controle, Texto, Borda, Fundo, Sucesso/Perigo/Alerta/Risco/Info, Sobreposição) — deve ser atualizada a cada token novo/alterado/removido daqui pra frente (registrado como convenção permanente). **Drift descoberto or corrigir depois:** a auditoria original (`color-audit-material-design.md`, Seção K) definia `Text/Success-Strong` = #005c1f e `Text/Primary` = #1a1a1a, mas o artefato real (usado dezenas de vezes, aprovado por Paulo ao longo de todo o rollout) usa #007a29 e #002a3a respectivamente — a Seção K do doc ainda não foi reconciliada com esses valores reais; fica como próximo passo. **Padrão de foco definido e aplicado:** campo de formulário (`.box`) = anel PRA DENTRO, colado na borda (offset -3px, já era assim); controle avulso (botão, checkbox, chip, card, aba, popover) = anel PRA FORA, sempre 2px de distância, sempre na cor de marca do DS (nunca a cor de chrome da própria ferramenta) — corrigidos `.close-btn` (tinha 1px, virou 2px), `.chk-real` (era colado, ganhou o gap de 2px), `.acc-mini__head`/`.tabs-mini__item` (usavam a cor de chrome da ferramenta por engano, agora usam a cor de foco do DS) — e **3 tipos de controle que não eram focáveis por teclado nenhum** (`.card-mini`, `.tn-tab`, `.frd-opt` reais) ganharam `tabindex`+ativação por Enter/Espaço.
- [x] **M61** [A11y][DS] **FEITO (2026-07-31) — hover de borda quebrado em 6 campos do artefato por causa de estilo inline.** Paulo pegou o `AthInputMoney` e o `EdsInputSelect` sem reação de hover; investigando, o `EdsInputSelect` já estava certo (sem bloqueio), mas 6 outros campos tinham o MESMO bug do `EdsButtonIcon` (M60): cor de borda fixada via `style` inline, que sempre vence a regra `:hover` do CSS. Corrigido em `AthInputDatepicker`, `AthInputMoney`, `NavSearch`, `AthInputYearpicker`, `AthInputTextArea` e `ChangePassword` — removido o inline, deixando a cor vir só da classe (`.col.proposed .box.state-default`), que a regra de hover (`Border/Hover`, M59) já consegue vencer por especificidade.
- [x] **M60** [A11y][DS] **FEITO (2026-07-31) — bug de interatividade real no EdsButtonIcon/EdsButtonRemove do artefato (não do produto).** `EdsButtonIcon` tinha o fundo fixado via `style` inline — nenhuma regra `:hover` em CSS consegue vencer inline sem `!important`, então o botão nunca reagia. `EdsButtonRemove` reaproveitava a classe `.btn.success-outline`, cujo hover só existe dentro do contexto `.col.current`/`.col.proposed` (comparação lado a lado) — fora desse contexto, nunca disparava. Corrigido com classes reais (`.btn-icon`, `.btn.danger-outline`) com hover/active/foco genuínos, sem depender de contexto.
- [x] **M58** ✅ **FEITO (2026-07-01→08-01)** [Ops][DS] **Redesign do layout do artefato `DS Color Study`.** Pedido: pesquisa no Mobbin (47 empresas referenciadas) + 5 propostas de croqui pra tornar a visualização mais profissional/minimalista (bastante espaço em branco, componente isolado, baixo esforço cognitivo): (A) busca/comando + slide único com pager, (B) editorial em rolagem única com sumário fino tipo Ditto/GitBook, (C) canvas+inspetor com toggle Hoje⇄Padrão no lugar (estilo Framer/Figma), (D) galeria em grade com status por bolinha → zoom isolado (estilo Relume/Etsy), (E) comparador narrativo 1 componente por vez, estilo "qual HomePod é o seu" da Apple. Paulo decidiu adiar — não é prioridade agora. Retomar quando ele pedir explicitamente; os 5 croquis completos (com decisões em aberto numeradas) estão no histórico do chat desta sessão, não replicados aqui. **[2026-08-01] ENTREGUE:** pesquisa no Mobbin feita (3 referências escolhidas e citadas: Mintlify, Arcade Brand Kit, Cloudflare), croqui aprovado pelo Paulo, e casca reconstruída — shell de 3 colunas (264/fluido/208, gutter 40), topbar sticky, busca com atalho `/`, item ativo em pastilha preenchida, cabeçalho de página com eyebrow + divisor, trilha "Nesta página", respiro dobrado. O estudo virou projeto próprio (`estudo-de-cores/`). Entregou 3 referências em vez das 5 propostas de croqui originalmente pedidas — o Paulo escolheu a direção no croqui único apresentado. Ver continuações 43-48 de `estado-atual.md`.

### Achados de código da auditoria de cores — os 12 lotes (M45–M57)

> **[2026-08-01] Estes achados foram CONSOLIDADOS e REVERIFICADOS** na
> `.claude/references/ds-color-implementation-spec.md` → **Parte 6**, com
> `arquivo:linha` conferido no código nesta data. **Trabalhe a partir da spec,
> não daqui** — um dos itens (o `#f8f8f8` em `BaseTemplate.vue:90`) estava
> impreciso aqui e foi corrigido lá. Estes itens seguem abertos como registro;
> a spec é a fonte de execução.

> Estes 13 itens **não são "fazer o lote"** — os 12 lotes estão prontos (109/109 componentes mapeados no artefato). São a **lista de correções que a auditoria revelou no código do <PRODUTO>**, um item por lote. Ficam abertos porque o repo é read-only pra nós e ninguém aplicou ainda. Estão agrupados aqui pra não inflar a leitura do backlog: quando alguém for mexer no código, atacar junto com **M64/M65/M66/M67** (que são da mesma natureza) — e o **M66 é bloqueador de todos os de foco**.

- [ ] **M57** [A11y][DS] **Lote 12 (FINAL) do estudo de cores por componente — rollout completo 109/109 (2026-07-31, 8 componentes: `AthInfinitLoader`, `AthFilterCessionStatus`, `AthCountDownTimer`, `AthCardWithButton`, `SegmentControl`, `AthTabsIcon`, `AthSkeletonInput`, família `Ath*Select*`).** (a) `AthFilterCessionStatus.vue` está vazio (0 bytes) — componente catalogado sem implementação; considerar remoção do índice do DS Agentic Stack numa manutenção futura. (b) `SegmentControl` usa uma paleta isolada (`#cf6700`/`#fff9f5`) sem relação com nenhum token do sistema — reprova como texto (3.60–3.75:1). (c) `AthSkeletonInput` confirma o mesmo cinza órfão do `AthSkeletonText` (`#e2e2e2`) — os 2 skeletons do produto, 1 correção resolve os 2. (d) `AthTabsIcon` tem fundo `#f3f1ff` hardcoded (mesmo valor do token Background/Brand-Subtle, só que cru) + roxo antigo como texto. (e) Família `Ath*Select*` fecha como o grupo mais limpo de toda a auditoria — 100% no sistema novo, confirmado pelo próprio índice do DS. **Artefato `DS Color Study` republicado com cobertura COMPLETA — 109 de 109 componentes.**
- [ ] **M56** [A11y][DS] **Lote 11 do estudo de cores por componente (2026-07-31, 10 componentes: `AthSpreadsheetDownloadSuccessPanel`, `AthSpreadsheetDownloadInProgressPanel`, `AthSkeletonText`, `AthSignersListInput`, `AthRibbon`, `AthProcessingLoader`, `AthPanelFinishedError`, `AthInputTextSearch`, `AthInputTextEditor`, `AthInputSlider`).** (a) **4ª ocorrência do overlay bypass:** `AthProcessingLoader` usa `rgba(0,0,0,.5)` — com o `AthModal`/`AthModalInformation` (`rgba(51,51,51,.5)`) e o `PhoneVerificationModal` (`rgba(0,0,0,.6)`), já são 4 componentes com overlay hardcoded e 3 valores de preto diferentes; candidato forte a 1 token `Overlay/Scrim` único. (b) `AthSkeletonText` usa `#e2e2e2`, um cinza isolado que não bate com nenhum dos 2 cinzas decorativos oficiais (`#f2f2f2`/`#f8f8f8`). (c) **Achado de tipo novo:** `AthInputSlider` usa um token de TEXTO (`Text/Primary-Inverse`) como cor de fundo decorativa do trilho — papel de token errado, funciona só por coincidência do valor renderizado ser um cinza claro neutro. (d) `AthInputTextEditor` sinalizado como RISCO não confirmado — a pele do editor Quill.js vem de CSS externo (`quill.snow.css`) + SCSS de outro módulo, fora do escopo desta auditoria de componentes do DS; precisa de rodada própria se for auditar. Artefato republicado, 101/109 componentes.
- [ ] **M55** [A11y][DS] **Lote 10 do estudo de cores por componente (2026-07-31, 10 componentes: `SecurityAccount`, `PhoneVerificationModal`, `NavSelectTenant`, `NavNotifications`, `ItemNotification`, `AthTimelineVertical`, `AthTextSelectedItems`, `AthSwipeMonths`, `AthSpreadsheetImportSuccessPanel`, `AthSpreadsheetErrorPanel`).** (a) **3ª ocorrência do overlay bypass:** `PhoneVerificationModal` usa `rgba(0,0,0,.6)`, um 2º valor de preto diferente do já usado no `AthModal`/`AthModalInformation` (`rgba(51,51,51,.5)`) — 3 componentes, nenhum usando o mesmo token, 2 tons de preto distintos. (b) `ItemNotification` repete literalmente o par já reprovado no `AthInputMultiCheckbox` (#8a76eb sobre #f3f1ff, 3.21:1) — mesmo bug copiado pra outro componente. (c) `AthTimelineVertical` — 3 de 8 status reprovam ícone (mesmo par recorrente: warning base 1.59:1, success 2.10:1), 3ª confirmação do mesmo par de falhas em componente de timeline. (d) `AthSwipeMonths` — mês selecionado em roxo antigo como texto comum, 3.58:1 (reprova 4.5:1). Artefato republicado, 91/109 componentes.
- [ ] **M54** [A11y][DS] **Lote 9 do estudo de cores por componente (2026-07-31, 10 componentes: `AthSpreadsheetInspectionPanel`, `AthPanelFinishedData`, `AthMultiCheckbox`, `AthInputTextFake`, `AthInputRangeSlider`, `AthInputMultiCheckbox`, `AthFilterSelectSchoolByCode`, `AthCopyTextBox`, `AthCardOverviewDetails`, `AthAlertBar`).** (a) **Padrão sistêmico, 5ª ocorrência:** `AthAlertBar` reproduz a mesma fórmula quebrada (fundo "light" + texto na cor base) já vista em `AthTag`, `_table.scss` (`AthPill`/`AthPanelFilesTableDownloadDelete`) e `AthBadge` — recomenda-se **1 correção arquitetural** (regra de token ou mixin `Text/*-Strong` sobre fundo `*-light`) em vez de seguir corrigindo componente a componente. (b) `AthInputRangeSlider` usa `#00865a` (verde) sem relação com nenhum token de sucesso — passa contraste (4.61:1) mas é um 6º valor solto de cor; junto, uma regra CSS morta (`background: yellowgreen`, só Firefox) que parece resquício de teste. (c) `AthCardOverviewDetails` — 3 de 6 variantes de círculo de status (ícone branco sobre cor) reprovam: sucesso (2.10:1), alerta (1.59:1), risco (2.85:1). (d) `AthCopyTextBox` — borda visível da caixa em `#e3e4e9` (1.27:1), mesma correção já aplicada em outros containers. Artefato republicado, 81/109 componentes.
- [ ] **M53** [A11y][DS] **Lote 8 do estudo de cores por componente (2026-07-31, 10 componentes: `EdsAlertGroup`, `ChangePassword`, `AthTreeJs`, `AthSecurityPassWord`, `AthProgressBar`, `AthLoader`, `AthInputMonthYearPicker`, `AthBadge`, `ModalMFAValidated`, `AthTreeJsMultiSearch`).** (a) **`--icon-color-input` nunca definida** — referenciada em 7 arquivos reais (`ChangePassword.vue`, `LoginForm.vue`, `ResetPasswordForm.vue`, `AcceptInvite.vue` ×2), inclusive telas de autenticação, mas ausente de todo tema; ícone de mostrar/ocultar senha usa cor herdada, não definida. (b) **`AthBadge` reproduz de forma independente** a mesma fórmula quebrada do `AthTag`/`_table.scss` (fundo "light" + texto na cor base) — 4ª aparição do mesmo anti-padrão em código que não compartilha arquivo; virou achado de padrão sistêmico, não mais 3 bugs isolados — considerar 1 regra de token que resolva os 3 de uma vez. (c) `AthLoader` usa um 4º hex de roxo diferente (`#baacff`), somando-se a `#ae9eff` (AthInputRadio/AthLoadingBar/AthProgressBar) e `#8a76eb` (roxo antigo) — 3 roxos "quase certos" coexistindo sem necessidade. (d) `AthTreeJs`/`AthTreeJsMultiSearch` — hover da árvore hardcoded (`#e0e0e0`) diferente do `Background/Hover` oficial (`#f2f2f2`) — drift visual, não falha de contraste. (e) `ModalMFAValidated` — ícone de alerta usa a cor "base" do warning (não a "-dark"), pior nível de warning já medido no estudo (1.59:1). Artefato republicado, 71/109 componentes.
- [ ] **M52** [A11y][DS] **Lote 7 do estudo de cores por componente (2026-07-31, 10 componentes: `AthPanelSheetSent`, `AthInputDiscount`, `ModalItemTwoParts`, `EbTippy`, `AthPanelFilesUpload`, `AthPanelFilesTableDownloadDelete`, `AthGuardianInfoModal`, `AthFormTitleList`, `TimelineRepasse`, `EdsInputMultiSelectSuggest`).** (a) **`TimelineRepasse`** — 3 estágios (pendente/concluído/em análise) com 3 vereditos diferentes pro mesmo ícone branco: pendente 1.27:1 (pior nível de ícone já medido no estudo — cinza claríssimo, rótulo do passo também usa a mesma cor como texto), concluído 2.10:1 (reprova), em análise 3.66:1 (já correto). (b) **3ª ocorrência do bug do `_table.scss`** (após `AthTag` e `AthPill`): badge de status "Enviado" em `AthPanelFilesTableDownloadDelete` mede 2.28:1. (c) `AthFormTitleList` — numeração do item ("#1") usa `Text/Secondary` como TEXTO real (não decoração), 1.81:1. (d) `AthGuardianInfoModal` — seta divisória decorativa também em `Text/Secondary`, 1.81:1 (ícone, precisa 3:1). (e) **2 componentes documentados como referência positiva** (já corretos, sem achado): `EbTippy` e `EdsInputMultiSelectSuggest` (100% no sistema novo de tokens). Artefato republicado, 61/109 componentes.
- [ ] **M51** [A11y][DS] **Lote 6 do estudo de cores por componente (2026-07-31, 10 componentes: `AthPill`, `AthModalHeader`, `AthDropButton`, `AthInputThemeDefault`, `AthCopyText`, `AthFormAddress`, `AthCardButtonSimple`, `AthModalInformation`, `AthLoadingBar`, `AthCardRadioButton`).** (a) **`AthPill` reproduz a fórmula quebrada do `AthTag`** (fundo `-light` + texto na cor base), mas puxada de um partial SCSS global (`_table.scss`) usado por 40+ nomes de status de tabela em todo o produto — corrigir esse arquivo cobre muito mais telas do que 1 componente isolado. (b) `AthModalInformation` repete o bypass de overlay já visto no `AthModal` (`rgba(51,51,51,.5)` cru em vez do token). (c) `AthLoadingBar` — preenchimento da barra (`--ath-color-primary-500` = `#ae9eff`, o MESMO valor que aparece hardcoded no `AthInputRadio`) mede 2.16:1 contra o próprio trilho, abaixo do mínimo de 3:1 pra elemento gráfico com significado. (d) `AthCopyText` — ícone de "copiado" (`Text/Success` #2ecc71) reprova como ícone (2.10:1). (e) `AthInputThemeDefault` ganhou aba própria — é o wrapper-base de quase todo campo de texto do produto, e é onde a regra "erro+foco" (M46) precisa ser aplicada de fato, não só demonstrada na aba de um input específico. Artefato republicado, 51/109 componentes.
- [ ] **M50** [A11y][DS] **Lote 5 do estudo de cores por componente (2026-07-31, 10 componentes: `NavSearch`, `AthPopoverSimple`, `AthInputYearpicker`, `AthTabsNormal`, `AthInputRangePicker`, `AthInputTextArea`, `AthFilterSelectSchools`, `AthInputPercentage`, `AthInputRadio`, `AthFilterReferencesDate`).** Primeiro lote do **rollout em massa** (Paulo autorizou seguir sem check-in por lote — revisão só no final). (a) `NavSearch.vue` — campo de busca da navbar remove o outline nativo e não substitui por nada: único campo de texto do produto sem indicador de foco algum. (b) `AthInputRadio.vue` — preenchimento do radio marcado é um 3º roxo hardcoded (`#ae9eff`, nem o antigo nem o novo), 2.30:1 contra branco, abaixo do mínimo de 3:1 pra indicador de estado. (c) **"Roxo antigo" (`#8a76eb`) confirmado em mais 4 lugares** (`AthPopoverSimple`, `AthTabsNormal`, `AthInputPercentage`, `AthFilterReferencesDate`) — mas com resultado que depende do contexto: reprova como texto comum 14-16px (Tabs/FilterReferencesDate), passa como "texto grande" 24px (InputPercentage) e passa como ícone isolado (Popover) — mesma cor, 3 vereditos diferentes, pela regra de tamanho da WCAG. (d) `AthInputYearpicker`/`AthInputRangePicker` — nenhum componente de data do DS define anel de foco customizado (mesma causa raiz nos dois). Artefato republicado, 41/109 componentes.
- [ ] **M49** [A11y][DS] **Lote 4 do estudo de cores por componente (2026-07-31, 10 componentes: `EdsButtonIcon`, `AthInputDragDropFiles`, `AthWizard`, `AthPopover`, `AthModalCenter`, `AthPanelFinishedSuccess`, `AthFilterTableSimple`, `AthTag`, `AthHr`, `AthCard`).** (a) `EdsButtonIcon.vue` — outline de foco referencia `var(--ath-color-primary-200)`, degrau que **não existe** na escala legada (só 50/100/500/700/900), custom property nunca resolve → foco quebrado silenciosamente. (b) `AthCard.vue` (hover/ativo) — mesma família de bug geométrico do foco do botão primário (M-A10-2): borda neutra vestigial sobre fill hardcoded saturado, nunca contrasta; fix não precisa de cor nova, só remover a borda no estado preenchido. (c) `AthTag.vue` — as 5 variantes semânticas (fundo `-light` + texto na cor base) reprovam todas, pior caso "success" 1.95:1; resolvido com a mesma troca pra `-Strong` já usada no Switch/Breadcrumb. (d) `AthWizard.vue` — ícone de "concluído" branco sobre verde legado de sucesso (`#2ecc71`) mede 1.88:1, reprova o mínimo de ícone (3:1). (e) `AthInputDragDropFiles.vue` — bug de interação: hover aplica `opacity:.5` na zona de drop por acidente, parecendo desabilitado no momento em que devia parecer mais ativo. (f) **Marco:** roxo errado `#8a76eb` chega à ~11ª ocorrência confirmada (agora em `AthTag`/`AthPopover`) — recorrência alta o suficiente pra justificar uma varredura global dedicada num lote futuro, em vez de esperar aparecer componente a componente. Artefato `DS Color Study` republicado, 31/109 componentes, novo grupo de menu "Layout" (`AthHr`/`AthCard`).
- [ ] **M39** [Ops][agentes] **Gap de cobertura: nenhum dos 13 agentes cobre "auditar componente do DS contra referência externa de mercado (Material/Ant/Polaris) + corrigir direto no Figma".** A10 (documentação) escreve conteúdo de doc mas não edita componente; A1 (código→Figma) só replica fiel, não audita; A4 (mapa do DS) é read-only. Na prática (2026-07-30) essa tarefa foi feita híbrida, no loop principal, sem spawn de nenhum agente definido. Considerar: extensão do A10 pra também poder editar o componente quando o gap for confirmado (com aprovação explícita), ou um agente novo dedicado a "auditoria comparativa + fix". Detalhe em `aprendizados.md` (2026-07-30, continuação 9). **Reforçado em 2026-07-30 (auditoria de cores):** mesma lacuna se repetiu pra uma auditoria de PALETA inteira (não só 1 componente) — ver M45.
- [ ] **M48** [A11y][DS] **Lote 3 do estudo de cores por componente (2026-07-30, 10 componentes).** (a) `AthBreadcrumb.vue:93` — bug real: `color="var(ath-color--secondary-400)"` sem o `--`, referência CSS inválida, ícone separador fica sem cor pretendida (achado de código, não de token). (b) `--ath-color-primary-700` #8a76eb ("roxo errado") chega à 9ª ocorrência confirmada e, medido como TEXTO, reprova contraste (3.58:1) no hover do breadcrumb e na aba ativa do `AthTabs` — não é só inconsistência de marca, é falha real de acessibilidade em pelo menos 2 lugares. (c) Foco já tem 3 mecanismos CSS diferentes pelo DS: `outline` (botão/input/select), ponto translúcido (Switch, M47), `box-shadow` (`AthInputCheckbox`) — dívida de consistência a considerar numa futura padronização. (d) `AthFormHeader` usa `.text-danger` puro do Bootstrap (#dc3545, passa raspando 4.53:1) em vez de um token do DS — 3º vermelho diferente no sistema. Ver `DS Color Study` (abas AthBreadcrumb/AthTabs/AthInputCheckbox/AthFormHeader).
- [ ] **M47** [A11y][DS] **3 achados novos do Lote 2 do estudo de cores por componente (2026-07-30).** (a) `AthInputSwitch.vue` — foco remove o outline (`outline:0 !important`) e mostra só um ponto `rgba(0,0,0,.25)` no thumb, 1.84:1 contra branco (bem abaixo do mínimo de foco 3:1); proposto trocar por `outline:3px solid Border/Focus`, igual botão/input (precisa de regra CSS nova, não só token). (b) `AthPanelWarning.vue` — ícones de `warning`(`--ath-color-warning-dark` #c5a533, 2.39:1) e `success`(`--ath-color-success` #2ecc71, 2.10:1) reprovam até o mínimo de ÍCONE (3:1), não só de texto — mais grave que o assumido na auditoria original. (c) `EdsInputSelect.vue:1082-1096` — mesmo RISCO do `AthInputText` M46 (classes `--focused`/`--negative` sem reconciliação se coexistirem); não confirmado se ocorre na prática (script não auditado), registrado como risco. Ver `DS Color Study` (aba EdsInputSelect/AthPanelWarning/Switch).
- [ ] **M46** [A11y][DS] **`AthInputThemeDefault.vue` — foco sobre campo com erro não tem regra própria (achado durante o estudo de cores, 2026-07-30).** `.has_error` (`:60-62`) força `border: 2px solid var(--border-danger) !important`; `&:focus-within` (`:87-90`) seta `border-color` + `outline: 3px solid var(--border-focus); outline-offset:-3px` **incondicionalmente**, sem checar se o campo já está em erro. Resultado real: campo com erro que recebe foco mostra a borda vermelha (do `!important`) com um anel de foco ROXO por cima, inset -3px — os dois brigam visualmente bem no momento em que o usuário está tentando corrigir o erro. Paulo notou isso ao interagir com a prévia (que reproduziu o CSS real fielmente) e perguntou o padrão de mercado. **Padrão comum em DS maduros (Material 3, Ant Design):** quando erro + foco coexistem, o anel de foco deve adotar a cor semântica do erro (não a cor de foco padrão da marca) — evita o choque de 2 acentos competindo e reforça "você está corrigindo isto agora". **Fix sugerido:** `.has_error:focus-within { outline-color: var(--border-danger); }` (ou equivalente) em `AthInputThemeDefault.vue` — não precisa de token novo, só uma regra CSS condicional que falta. Fix já aplicado na prévia (`DS Color Study`, aba AthInputText) pra ilustrar; aplicação real no componente é decisão/execução futura do time.
- [ ] **M45** [A11y][DS] **Auditoria completa de cores <PRODUTO> vs. Material Design 3 (2026-07-30)** — doc `.claude/references/color-audit-material-design.md` + artefato interativo publicado ("DS Color Playground"). Confirma e QUANTIFICA com números reais M2/M5/M-A10-1/M-A10-2/M-A10-10/M-A10-11 (ver anotações nesses itens). Achados novos não cobertos antes: (a) `--border-primary` (neutral-300 #b2b2b2) falha 3:1 contra os DOIS fundos reais do produto (card branco 2.12:1 e canvas #f8f8f8 1.998:1) — proposto `neutral-450` #8c8c8c (3.17–3.36:1); (b) `--border-warning` #cca300 é o único border semântico reprovado hoje (2.38:1) — precisa `yellow-700`/`yellow-800`; (c) `--background-notice` (laranja) é o pior caso de fill sólido (2.25:1, precisa 2 degraus até `orange-700`, não 1); (d) regra sistêmica nova: toda `background-*-subtle` (badge) só deve casar com `content-*-bold`, nunca com a cor base — cobre 5 badges (danger/success/info/warning/notice) de uma vez, sem mudar valor; (e) o fundo cinza real do app (#f8f8f8, `BaseTemplate.vue:90`) nunca foi formalizado como token semântico — só existe via `--ath-color-light` legado + `--app-background-color` quebrado (referencia `--ath-link`, nunca definido em lugar nenhum do repo); (f) **o sistema legado `--ath-color-*` (usado em 324/403 arquivos, o dobro do novo) reprova em 7 de 10 tokens semânticos medidos** — é sistematicamente pior que o sistema novo e é onde está o maior volume de risco de acessibilidade real em produção hoje. Aplicação dos valores no código é decisão/execução futura do time (<PRODUTO> é read-only pros agentes). **Rodada 4 (2026-07-30, composição real em 5 telas — Faturas/Turmas/Home/Negativação):** confirmou que a borda mais usada de fato é a LEGADA `--ath-border-color`/`--ath-color-secondary-300` (#e3e4e9), não a nova `border-primary` — as duas foram unificadas num único `Border/Default`. O maior risco real está no badge de status `AthTag` (usado em Faturas/Home/Negativação): reprova em todos os 5 estados reais medidos direto do componente, "Paga" no pior nível de toda a auditoria (1.95:1). Achado à parte (não é fix de token): `AthCard.vue` (hover/active) tem borda neutra clara sobre fill roxo sólido hardcoded — vestigial, precisa de toque de código no componente, não só troca de valor de token. Paleta final unificada e nomenclatura designer-friendly (`Text/Background/Border Color`) entregues como lista no chat; ver `color-audit-material-design.md` seções I/J. **Rodada 5 (2026-07-30, rollout por componente, Lote 1/5):** varredura mecânica nos 109 componentes do índice do DS Agentic Stack achou 17 com hex hardcoded (Grupo B; limitação: só pegou `#hex`, não `rgb()/rgba()` — achado manual extra no overlay do `AthModal`, `rgba(51,51,51,.5)` bypassando `--overlay-50`). Publicados 5 artefatos (Hoje vs. Nova Paleta) pros componentes mais usados: `EdsButton`, `AthModal`, `AthInputText`, `EdsTable`, `AthIcon`. Achado extra: `AthIcon` revelou 2 roxos de "marca" diferentes coexistindo sem decisão consciente (`#8a76eb` do `AthCard` vs `#ae9eff` legado) — a unificação resolve os dois de graça. Próximo: Lote 2/5 (`AthFormTitle`, `EdsInputSelect`, `AthTitle`, `AthPanelWarning`, `AthAccordion`), sob pedido do Paulo.
- [ ] **M1** Grade de **spacing/radius do DS incompleta** (escala `Global tokens` termina em 26; sem 1/5/6/28/32) → força variáveis locais em quase toda tela. Propor completar a escala ao time de DS.
- [ ] **M2** **Drift de cor código↔DS:** danger #e50000 vs #eb5757; warning #efca44 vs #e3b200; warning-dark #c5a533 sem token; border-primary #b2b2b2 vs Figma #737373. Alinhar (elimina estilos locais). **Quantificado em M45:** border-primary reprova 3:1 nos dois fundos reais (2.0–2.12:1); proposto neutral-450 #8c8c8c.
- [ ] **M5** [A11y] **Cores de feedback do DS falham contraste** (badges danger/warning < mínimo WCAG). Revisar os valores do DS. **Quantificado em M45:** todo par `*-subtle` + cor base falha (2.1–4.0:1); trocar por `*-bold` resolve 4 dos 5 badges sem mudar valor, só o `success` precisa também trocar o bold pra green-700.
- [ ] **M8** **Componentes que faltam no DS** (viraram custom/local): (a) badge/tag de status semântico (5 status); (b) Select/Yearpicker em modo filtro compacto; (c) card-métrica (ícone+título+contagem+R$+desc); (d) action-button pill outline (variante link/filtro do Button).
- [ ] **M9** [infra] Componentes do DS **novo usam `Satoshi Variable`** (ausente no ambiente do plugin) → editar texto exige refont p/ `Satoshi`. Instalar a fonte ou padronizar o DS em Satoshi.
- [x] **M-A11-1** [A11][onboarding] **FEITO (2026-07-20):** README + COMECE-AQUI atualizados para **12 agentes** (faltava o A12 `codigo-ao-video` na vitrine, na tabela do elenco e na estrutura de pastas; contagem estava em 11). Nota (drift menor, não corrigido agora): o COMECE-AQUI numera o Anfitrião como "A0" enquanto o CLAUDE.md usa A11 — didático, mas inconsistente; alinhar numa próxima. ~~README dizia "6 habilidades" / 11 agentes~~.
- [ ] **M-A10-1** [A11y][A10] **EdsButton — 5 variantes falham contraste WCAG AA** (medido, texto normal 4.5:1): `success` #F2F2F2/#009933 = 3.35; `danger` #F2F2F2/#EB5757 = 3.11; `success-outline` #009933/#fff = 3.75; `danger-outline` #EB5757/#fff = 3.48; `link-danger` #EB5757/#fff = 3.48. Escurecer o token de fundo (success/danger) ou o de texto (outlines/link). Relaciona-se com M5/M2.
- [x] **M-A10-2** [A11y][A10] **RESOLVIDO NO ESTUDO (2026-07-31, versão final — anel duplo, sem sair do roxo de marca):** EdsButton — anel de foco quase invisível sobre fill de variante sólida (`--border-focus` brand-600 #5644AD sobre brand-500 #6B55D8 = 1.39 < 3.0; `outline-offset: 0`). 1ª tentativa (cor única, inclusive testando azul) provou que NENHUMA cor sólida tem 3:1 contra branco E contra os preenchimentos de danger/success/brand ao mesmo tempo — é geometria (anel encostado no fill), não cor. Paulo pediu pra manter o roxo de marca em vez de azul e revisar 2 referências antes de decidir — pesquisa confirmou a técnica de "anel duplo"/halo (Sara Soueidan, "A guide to designing accessible, WCAG-conformant focus indicators"; Access Armada, "Designing High Contrast Focus States"): 2 traços concêntricos, um claro (contrasta contra fundo escuro/saturado) + um escuro (contrasta contra a página), cada um só precisa contrastar com o vizinho dele, nunca com os dois extremos ao mesmo tempo. **Fix final (só nas variantes preenchidas — primary/danger/success):** anel interno `brand-100` #e1ddf7 (4,1–5,3:1 contra qualquer preenchimento saturado) + anel externo `Border/Focus` #5644ad, inalterado (7,4:1 contra a página) — os dois já existiam na escala, zero cor nova, 100% dentro da marca. Variantes de contorno (ex. success-outline, já com fundo branco) mantêm o anel único de sempre. Ver `DS Color Study` (aba EdsButton). Aplicação real no código é decisão futura do time.
- [ ] **M-A10-3** [A11y][A10] **EdsButton `loading` sem anúncio** — não expõe `aria-busy` nem rótulo acessível pro spinner `circle-notch`; leitor de tela não avisa do processamento.
- [ ] **M-A10-4** [A10] **EdsButton token-role errado** — variante `link` usa `--background-brand` como cor de **texto** (`EdsButton.vue:219`); deveria ser `--content-link`/`--content-brand`.
- [ ] **M-A10-5** [A10] **EdsButton CSS possivelmente morto** — `.icon-right` (absoluto), `.btn-create`, `.fit-content`, `.border-0` não são acionados pelo `:class` do template. Confirmar uso externo via `$attrs` ou remover.
- [ ] **M-A10-6** [A10] **EdsButton `padding !important` na raiz** (`.eds-button { padding: 0 0.8rem !important }`, :125) pode sobrescrever o padding de `sm`/`lg` (sem `!important`). Verificar no navegador.
- [ ] **M-A10-7** [A9][cobertura] **EdsButton — faltam stories** de `link-secondary` e `link-danger` (existem no enum, sem story dedicada).
- [ ] **M-A10-8** [A4] **`design-tokens.md` lista `--border-focus` em duas linhas** (Brand/600 e Blue/200); o código define `--border-focus = $brand-600` (`root.scss:87`). Corrigir a linha do Blue/200 no mapa.
- [ ] **M-A10-9** [A10][DS] **DS sem changelog/maturidade por componente** — nenhum label semver/`@since` no código. Adotar versão+changelog por componente (modelo na doc do EdsButton §16).
- [ ] **M-A10-10** [A10][DS] **AthInputCheckbox — borda em repouso `#898989` hardcoded** (`AthInputCheckbox.vue:122`), sem token. Tokenizar (`--ath-border-color`/equivalente EDS) e checar contraste do controle (WCAG 1.4.11, mínimo 3:1) sobre branco.
- [ ] **M-A10-11** [A10][DS] **AthInputRadio — radio marcado `#ae9eff` hardcoded** (`AthInputRadio.vue:180-181`), lilás claro sem token. Migrar para `--ath-color-primary-*` e checar contraste do indicador selecionado.
- [ ] **M-A10-12** [A9][A10][infra] **AthInputMoney inerte no Storybook** — preview central não provê `inject('format')` nem registra a diretiva `v-money`; digitar/iniciar com valor lança `TypeError` e o input renderiza sem máscara. Adicionar `provide('format')` + registrar `v-money` no preview (já sinalizado no topo da story).
- [ ] **M-A10-13** [A11y][A10] **Inputs legados `Ath*` (checkbox/radio/money) sem `aria-live`/`aria-describedby` na mensagem de erro** — o erro é só visual (`.span-error`/`has-error`); leitor de tela não é avisado. Ligar mensagem ao campo e anunciar (padrão do EdsInputSelect, que já usa `aria-live`).
- [ ] **M10** **`AthCard` usa hex hardcoded** (#2b2256/#6b55d8/#adbbff, marcados `//TODO`) → migrar p/ tokens do DS novo (`background-brand(-pressed)`, `border-focus`) e adotar o `Card - Button` do DS.
- [ ] **M14** [A2/A1][infra] **Componentes do DS não instanciáveis por key** — `importComponentByKeyAsync('8ea3…' Button)` retorna "not found" (2026-07-17); a maioria dos componentes ainda é WIP/não publicada na lib. Efeito: em propostas/réplicas não dá pra **instanciar o componente real** (reuse-first de verdade, Lição 5) — cai-se em construir com tokens. Ações: (a) confirmar/atualizar as **keys** no reuse-map após publicação; (b) publicar ao menos os 🟢 em produção (Button/ButtonIcon/Select) + Alert/Pill/Badge p/ o A2 conseguir instanciar; (c) alternativa: localizar o componente por traversal na página e `clone()` dentro do próprio arquivo DS. **[2026-07-19, A4]** Camada 1 do contrato (`components.contract.json`) já preenchida p/ os 3 🟢 com as hashes gravadas como `candidateSetKey` (não verificadas) — falta **só publicar/confirmar** essas 3 published set keys no Figma p/ a instanciação funcionar. Nuance técnica p/ o teste: a hash é de **COMPONENT_SET** → usar `importComponentSetByKeyAsync`, não `importComponentByKeyAsync`. **[2026-07-19] ✅ KEYS CONFIRMADAS ao vivo** (`search_design_system`): Button `8ea3eaff…`, Button icon `08857119…`, Select Fluid `faa42e3f…` + select_option `8c721741…` — todas batem com os candidatos e foram gravadas como `setKey` no contrato (lib `lk-dec114…`). **[2026-07-19] ✅ INSTANCIAÇÃO PROVADA** (frame "Testes Hi Fi" `4727:7440`): `importComponentSetByKeyAsync` importou os 3 como COMPONENT_SET — Button (126 variantes), Button icon (45), Select (11). **M14 100% RESOLVIDO.** ~~Falta o teste de instanciação~~.
- [ ] **M15** [A4→DS][a11y] **Foco inconsistente entre EdsButton e EdsButtonIcon** — `EdsButton` usa o token novo `--border-focus` no anel de foco (`:focus-visible`), mas `EdsButtonIcon` usa o legado `--ath-color-primary-200` + `box-shadow` com `--ath-color-primary-rgb`. Dois botões do DS novo com foco visualmente diferente. Padronizar o `EdsButtonIcon` para `--border-focus` (achado ao preencher o contrato, 2026-07-19).
- [x] **M17** [A3/A10/a11y] **TRIADO (2026-07-20)** → relatório `reports/auditoria-contrato-ath-2026-07-20.md`: os achados de **bug/comportamento quebrado/PII** foram **verificados 1×1 contra o código** por 5 verificadores céticos (read-only, arquivo:linha) — **20 confirmados, 1 refutado** (nenhum aceito sem evidência). Prioridade: 🔴 4 alta (AthPanelSheetSent `<style>` sem scoped; AthModalInformation `document.onkeydown` sobrescreve global; AthInputMultiCheckbox muta `props.options`; AthGuardianInfoModal PII crua + a11y ausente) · 🟡 10 média · ⚪ 7 baixa — cada um com correção sugerida. Os achados de **a11y/token-cru/CSS-morto** (padrão sistêmico) ficam na **régua M34** (sweep, não ticket 1×1). Histórico bruto pré-verificação abaixo:
- [ ] **M17-raw** [histórico] **Lote de achados a11y + CSS-morto dos 23 top-usados** (extração do contrato, 2026-07-19 — ⚠ **a confirmar**, sinalizado por extratores Sonnet as-is, não verificado a fundo). **a11y:** `ath-input-switch` sem `role=switch`/`aria-checked`; `ath-modal` sem `role=dialog`/`aria-modal`/focus-trap; `ath-tab`/`ath-accordion` sem role/operação por teclado; `ath-title`/`ath-form-title`/`ath-form-header` em `<div>` sem heading semântico; `ath-loading` sem `aria-live`; `ath-icon` sem `aria-label` na maioria dos usos; `ath-copy-text`/`ath-breadcrumb`/`ath-input-drag-drop-files` com `<div>` clicável sem teclado. **Contraste (WCAG):** sub-title `#7d8097` 3.88:1, breadcrumb links 3.88/3.58:1, switch border `#e3e4e9` 1.27:1, hr 1.27:1. **CSS morto / bugs:** `ath-input-switch` `.ul`; `ath-breadcrumb` `.ath-breadcrumb-border/-primary`; `ath-panel-warning` `.container-{type} em` mira `<em>` inexistente (acento por `type` quebrado); `eds-table` `showBarsAction`; `ath-input-drag-drop-files` estado `fail` nunca setado; **bug** `AthIcon` prop `require` (typo de `required`, `AthIcon.vue:28`). **Ação:** A3/A10 verificam caso a caso ANTES de virar tarefa; não agir cego. Detalhe no output do workflow `contrato-top25-componentes`. **[2026-07-20 — fatia 3 do contrato, +15 componentes] Novos achados (⚠ a confirmar):** **bugs** — `NavSearch` `default:()=>{}` retorna `undefined` (provável erro; autor quis `()=>({})`, NavSearch.vue:37); `AthCardRadioButton` compara `title=='undefined'` (string literal, :102); `AthTabsNormal` classe `.tag-new` no SCSS não referenciada. **a11y** — `NavSearch` remove foco (`outline:none` sem `:focus` substituto, viola WCAG 2.4.7) + placeholder como único label; `AthCardRadioButton` esconde o radio nativo com `display:none` (fora da árvore de acessibilidade/teclado) + `name="selected-item"` fixo colide entre grupos; `AthModalCenter` sem `role=dialog`/`aria-modal`/focus-trap; `AthTabsNormal`/`AthPopoverSimple`/`AthFilterTableSimple` = `<div @click>` sem role/teclado; `AthInputTextArea`/`AthInputYearpicker`/`AthInputRangePicker` erro sem `aria-live`/`aria-describedby`; `AthPill`/`AthTag` ícone de status sem alt. **drift de token (hex cru)** — `AthInputRadio` `#ae9eff` (confirma M-A10-11), `AthWizard` `#cccccc` (disabled), `AthModalCenter` `#ffffff` (em vez de `var(--ath-color-white)`). **DS/estrutura** — `AthTag` size `sm/md` só via class fallthrough (não prop formal → deveria virar prop); `AthPill` ~90 valores de status via `type` string livre, e o par 'Pill' do Figma está **NÃO casado** no reuse-map (divergência estrutural provável: Figma Pill = tag genérica interativa × AthPill = badge de status). A4 deve confirmar o par Pill↔AthPill ao vivo. **[2026-07-20 — fatia 4 do contrato, +15 (41→56)] Novos achados (⚠ a confirmar):** **bugs** — `AthFormAddress` autofoco do CEP quebrado (`getElementById('cep')` nunca acha; input usa `name="cepAddress-${id}"`, sem `id="cep"`, :83); `AthProgressBar` var CSS `--progress` da keyframe **nunca definida** (animação desconectada do width real, :49); `AthInputDiscount` non-null assertion sobre prop opcional → **NaN silencioso** (:104) + CSS órfão `.multiselect-input` (:178-184); `AthPanelSheetSent` **`<style>` sem `scoped`** vazando `.eds-button>span`/`.ath-text-h2` global (:196) + botão "CancelInspect" sem handler; `AthSkeletonText` style global não-scoped; `AthBadge` `iconColor` duplicado (JS computed vs CSS `::before`); `AthTabs` prop `notification` declarada mas nunca lida. **a11y** — modais (`AthModalInformation`) sem `role=dialog`/`aria-modal`/focus-trap + fechar sem teclado + `document.onkeydown` sobrescreve handler global; progressbars (`AthProgressBar`/`AthLoadingBar`) sem `role=progressbar`/`aria-valuenow`; `AthDropButton`/`AthTabs`/`AthFilterReferencesDate` = opção/trigger sem role/tabindex/teclado (`aria-expanded` no dropdown); `AthSkeletonText` sem `aria-hidden`/`aria-busy`; **animações sem `prefers-reduced-motion`** (loading-bar, skeleton); `AthPanelFilesUpload` sem `aria-live` ao add/remover arquivo; `AthCard` clicável sem role/tabindex/keydown; `AthModalHeader`/`AthCardWithButton` título em `<div>` sem heading. **drift de token (cru)** — `AthDropButton` `background:white` (:164); `AthModalInformation` `#fff`/rgba de sombra/overlay; `AthProgressBar` `border-radius:50px`; `AthModalHeader`/`AthCardWithButton`/`AthFilterReferencesDate` px/rem crus; `AthCard` 3 hex com TODO; `AthSkeletonText` hex/rgba sem CSS var. **NOVO ITEM — família de selects como 1 padrão:** os ~36 `Ath*Select*` (wrappers finos do `EdsInputSelect`) devem entrar no contrato como **1 item de padrão + lista de instâncias**, não 36 entradas. A10 já mapeou a família no Storybook; A4 formaliza no contrato quando priorizar. **[2026-07-20 — fatia 5 do contrato, +16 (56→72)] Novos achados (⚠ a confirmar):** **PII/segurança** — `AthGuardianInfoModal` expõe nome/CPF-CNPJ/email/telefone/endereço do responsável **sem mascaramento** + tag órfã `<ath-tag/>` sem props (leftover, :93). **anti-padrão Vue** — `AthInputMultiCheckbox` **muta `props.options`** direto (:69/84/154, viola one-way flow) + Field name fixo "multiSelectBox" colide se usado 2×; `AthMultiCheckbox` risco de `id`/`for` duplicado (colisão `name`/`label`); `AthPanelFilesTableDownloadDelete` trigger "MoreAction" fixo. **props contraditórias/mortas** — `AthCardOverviewDetails` `textTitle` `required:true`+`default:''` ao mesmo tempo (:41) + `iconStatus`/`color` sem vínculo (risco ícone×cor divergentes); `AthInputMonthYearPicker` JSDoc promete `errors:Array|String` mas prop só aceita `String` (:17 vs :53); `AthLoader` prop `size` declarada e **nunca usada**. **a11y** — `AthInputTextFake` finge input editável mas é `<div>` sem `role`/`aria-readonly`/label→valor (engana leitor de tela); `AthInputRangeSlider` sem `aria-label` min/max; `AthSecurityPassWord`/`AthPanelFinishedData`/`AthPanelSpreadsheetInspection` sem `aria-live`; `AthAlertBar` sem `role=alert`; `AthCopyTextBox`/`MoreAction` `<div>` clicável sem teclado. **drift/CSS morto** — `AthLoader` `#baacff`/`rgba(...)` cru; `AthInputRangeSlider` CSS morto (`.minmax-indicator`/`.default`, hex `#00865a`/`yellowgreen`) + white cru; `AthPanelFilesTableDownloadDelete` CSS morto; `AthSpreadsheetInspectionPanel` usa `--ath-font-size--headline-md` como line-height (:109); `eds-input-multi-select-suggest` (único **new** da leva) font cru na :191-194.
- [x] **M38** [A4/DS] **Contrato de componentes chegou a COBERTURA 100% (2026-07-25): 72 → 109 entradas, 166/166 arquivos contemplados.** Pedido do Paulo: terminar o que tinha parado em 72 (decisão de 2026-07-20 de parar ali e virar régua de qualidade — M34) e chegar a 100%, "código nasce primeiro, Figma depois". Método: 10 agentes paralelos (Fase 1, código) + merge determinístico + 5 agentes paralelos (Fase 2, Figma ao vivo nas 2 bibliotecas ativas via `search_design_system`, nunca a legada) + merge por upsert. **Decisões de escopo comunicadas (reversíveis):** família `Ath*Select*` (35 arquivos, confirma a nota da fatia 4 abaixo) e `AthInputTheme*` (5 arquivos, camada de tema injetável via `<component :is="props.theme">`) viraram **1 entrada de padrão cada**, não 35+5 entradas; 15 arquivos são partes internas privadas (documentadas dentro do pai: `eds-table`/`DataTable*`, `eds-alert-group`/`AlertItem`+`AlertSummaryHeader`, `ath-tree-js`/`lib/*`); 4 arquivos (`BaseTemplate`/`NavBar`/`SideBar`/`MenuItem`) são o chrome único do app, excluídos do contrato de componentes. **Resultado Fase 2:** só 1 match Figma real novo (`eds-alert-group` ↔ Figma "AlertGroup", key `7b29542f...`, biblioteca nova); os outros 35 confirmados `code-only` com a pesquisa registrada (nunca presumida) — confirma que a cauda longa do DS é mesmo majoritariamente só-no-código. **Achados de auditoria relevantes (⚠ a confirmar, não corrigidos):** 3 componentes **órfãos/código morto** (zero uso real no `webclient`) — `AthTabsIcon`, `SegmentControl`, `AthPopover` (este último tinha ficado de fora até da varredura inicial, corrigido na conferência final de cobertura); **bug de wiring** `AthTreeJs`/`AthTreeJsMultiSearch` escutam `@update` mas o `TreeNew` só emite `update:nodes` — handler nunca dispara; **transição CSS Vue 2 num projeto Vue 3** (`AthInfinitLoader`, classes `.fade-enter`/`.fade-leave-to` que não existem mais na sintaxe do Vue 3 — animação quebrada silenciosamente); **PII/segurança séria** — `ModalMFAValidated` exibe os 6 códigos de recuperação MFA em texto pleno, sem mascaramento/timeout; **violação de reuse-first** — `PhoneVerificationModal` implementa seu próprio overlay do zero (Teleport) em vez de reusar `ath-modal` como os componentes irmãos (`ChangePassword`/`ModalMFAValidated`/`SecurityAccount`); **gap de a11y sistêmico e transversal** — nenhum dos 5 temas de `AthInputTheme*` associa `label`↔`input` via `for`/`aria-labelledby` (afeta todo input que usa a camada de tema, não só 1 componente); **arquivo morto** — `webclient/src/components/filters/AthFilterCessionStatus.vue` tem 0 bytes, duplicado do componente real em `modules/cessions/`. Candidatos fortes a entrar na régua **M34** (migração Ath→Eds) na próxima revisão. Detalhe completo em `.claude/references/ds-contract/components.contract.json` (campo `notes` de cada id) e `aprendizados.md`. **Atualização (mesmo dia):** os 9 achados relevantes foram levados pra tabela "Riscos" do painel-eficiencia (`painel-eficiencia/js/app.js`, array `RISKS`, 20→29) e pros números da Visão Geral/hero/legenda (`index.html`) — alta 3→4, média 10→13, baixa 7→12. **Bug real achado na validação visual (Playwright):** a página dizia "Ordenados por gravidade" mas `renderRiscos()` nunca ordenava de verdade — só funcionava porque os 20 originais já tinham sido digitados em ordem; os 9 novos, appendados no fim, quebraram a ordem visível. Corrigido de vez com um `.sort()` real por gravidade (`SEVORDER`), não é mais um acidente de digitação. Validado ao vivo (Playwright headless via cache do npx, já que não havia instalação própria no projeto): 29 linhas, 4/13/12 na ordem certa, 0 erros de console.
- [x] **M34** [A4/A9/A10/estruturante] **Checklist de migração Ath→Eds — v1 CRIADA (2026-07-20):** `.claude/references/ds-migration-checklist-ath-to-eds.md` (6 categorias: a11y, tokens, código Vue, CSS, estrutura DS, privacidade; cada item com caso real das fatias 3-5). Ligada nas skills A9 (reportar) e A10 (seção a11y/do's&don'ts). **Régua viva** — cada nova auditoria adiciona item. PENDENTE (evolução): A4 pontuar componentes contra a régua (score de migração no tempo); virar seção do Storybook/doc do DS. — a leitura de ~40 componentes legado (contrato fatias 3-5) revelou uma **dívida sistemática e previsível** do `Ath*`, não bugs isolados: (1) a11y ausente (sem role/teclado/aria-live, animação sem `prefers-reduced-motion`), (2) token cru (hex/px hardcoded), (3) anti-padrões Vue (mutação de props, Field name fixo, prop contraditória/morta, `<style>` sem `scoped`), (4) CSS morto. Virar isso num **checklist repetível** (o que todo componente migrado precisa cumprir) transforma a lista de achados espalhados numa **régua de qualidade** pro DS novo. Base: os achados M17 + o mapa de família de selects do A10.

- [x] **M40** [A1][painel-eficiencia] **FEITO (2026-07-25):** Tabela "Consistência" fechou 100% dos dois lados — Storybook 72/72 e Figma 72/72 (os 51 componentes que faltavam foram todos desenhados na biblioteca real, em 2 rodadas priorizadas por uso real no código; ver `aprendizados.md` continuações 16/17). Nada mais pendente aqui.
- [ ] **M41** [A3/A4][DS] **Achados de auditoria REAIS descobertos ao ler o código dos 51 componentes pra desenhá-los no Figma (2026-07-25) — candidatos a bug real, nunca corrigidos (as-is, só documentados).** Destaques mais sérios: (a) `AthPanelSheetSent` tem `&lt;style&gt;` **não-scoped** que vaza globalmente `.ath-text-h2` e `.eds-button > span` pro app inteiro; (b) `AthGuardianInfoModal` renderiza `&lt;ath-tag /&gt;` sem NENHUMA prop — sempre mostra "Principal" estático, sem lógica real ligada a `isDefault`/`isVerified`; (c) `AthInputRangeSlider` não tem fill visual do intervalo selecionado (o único CSS que pintaria isso, `.minmax-indicator`, está num elemento nunca inserido no DOM — código morto, possível bug de renderização real); (d) `AthInputRangePicker`/`AthInputMonthYearPicker` (filtro): a prop `errors` só troca a borda pra vermelha, **não mostra a mensagem de erro** — motivo do erro fica invisível; (e) `AthMultiCheckbox` nunca propaga `disabled`; (f) `AthAlertBar` usa a cor sólida da borda como `box-shadow` (sem alpha) — sombra vai renderizar "dura", não suave; (g) `AthCard` tem 3 comentários `//TODO: usar token X do novo DS` cujos tokens **já existem hoje**, mas os hex hardcoded não batem mais com os valores atuais (drift). Lista completa (mais ~15 achados menores) nas respostas dos agentes de pesquisa dessa tarefa — vale uma passada do Agente 3 pra confirmar/priorizar antes de expor num board de bugs.
- [ ] **M74** [A3/A4][painel-eficiencia] **9 achados da extração 2026-07-25 sem o mesmo detalhamento dos 20 verificados (M17).** Contexto: a tabela "Riscos" ganhou um painel lateral (2026-07-25) que mostra "Problema"/"Correção sugerida" por extenso ao clicar na linha — pra isso, corrigi os 20 achados do `reports/auditoria-contrato-ath-2026-07-20.md` que estavam truncados a ~100 caracteres no meio da palavra (array `RISKS` de `painel-eficiencia/js/app.js`), usando o texto integral do relatório. Os outros 9 (`ModalMFAValidated`, `AthTreeJs`/`AthTreeJsMultiSearch`, `AthInfinitLoader`, `AthInputTheme*`, `AthTabsIcon`, `SegmentControl`, `AthPopover`, `PhoneVerificationModal`, `AthFilterCessionStatus`, vindos do M38) já eram frases completas (não truncadas), mas são mais curtas/telegráficas — nunca passaram pelo crivo cético de 5 verificadores que os 20 do M17 tiveram. O painel lateral deles funciona, só mostra um texto mais magro. Quando esses 9 passarem pela mesma verificação 1×1 (A3), atualizar `q`/`fix` no mesmo array. *(era **M39** — renumerado em 2026-08-01 porque o ID estava duplicado; o outro item com esse número é citado em outros docs e ficou com ele.)*

## Ops / Novas capacidades
- [x] **M37** [Ops][Rulebook→painel-eficiencia] **RESOLVIDO (2026-07-25):** o painel-eficiencia inteiro (não só a sidebar) foi convertido pro padrão shadcn (mesma escala 256/48px de sidebar, radius/tipografia/espaçamento) num pedido maior do Paulo pra adotar 100% os componentes do shadcn — ver `estado-atual.md` 2026-07-25. ~~Sidebar do Rulebook reconstruída (2026-07-24) — painel-eficiencia ainda na escala ANTIGA~~. Pendente só a validação visual do Paulo.
- [ ] **M35** [Ops/agente-candidato] **Agente "Publicador Seguro" (A13 candidato)** — destilado de guiar o Paulo (cobaia, 2026-07-21) a publicar o Rulebook protegido. **Papel:** pega qualquer página estática (local/online) e guia o usuário — baby steps, voz de designer — até publicá-la num espaço **protegido** (só o time acessa), como um **protocolo de segurança** do Hub. **PROTOCOLO validado (sem cartão):** (1) preparar pasta estática (`public/` com index+assets); (2) Cloudflare → Workers & Pages → Create application → **"Upload your static files"** → **FINALIZAR o deploy** (se sair antes, o projeto some); (3) acesso restrito **sem cartão** = **Basic Auth via Worker** com `run_worker_first:true` (worker.js checa user+senha antes de `env.ASSETS.fetch`; senha em `wrangler secret put SITE_PASSWORD`, nunca no repo); deploy por `npx wrangler login/deploy/secret` (Node já instalado); (4) testar (401 sem senha) + **apagar deploys públicos intermediários** (faxina). **Curvas que o agente deve prevenir (fricções reais observadas):** o menu lateral **"Domains" = comprar domínio** (beco; o usuário caiu 2×) ≠ abas do projeto; "Upload assets" cria **Worker** (não "Pages"); **Zero Trust Access pede CARTÃO** no free → descartar se o usuário recusa; **email-OTP** só é seguro **com política de domínio** e ainda assim gera desconfiança (auto-serviço); **`run_worker_first` é CRÍTICO** (sem ele os assets servem antes do auth → vaza); deploy intermediário fica **público** até trancar (não divulgar a URL). **Trade-off do Basic Auth:** senha **compartilhada** (não login individual) — para login individual `@<empresa>.com.br` **sem cartão**, caminho é **TI/SSO corporativo** (Google Workspace). **Resultado real:** `livro-de-regras.paulo-silva-52c.workers.dev` (Basic Auth, user `<empresa>`). Ver [[rulebook-regras-negocio]] + aprendizado [Ops] 2026-07-21. Avaliar com a régua do crítico (papel/gatilho/tools distintos + reuso amplo) se vira 13º agente ou skill.

- [ ] **M43** [Ops][painel-eficiencia] **`publicador-seguro` (A13) fora da contagem de adoção** — nunca foi adicionado a `AGENTES_HUB`/`SLUG_TO_KEY_PAINEL` (`telemetria.py`) nem `SLUG_TO_KEY` (`gerar-adocao.py`), então uso real dele não aparece no painel (nem via spawn/skill, nem via o detector de intenção novo — o span `IntentDetected` é gravado mas ignorado no rollup por falta de mapeamento). Achado durante a implementação do roteador de intenção (2026-07-30), fora do escopo daquela tarefa.
- [ ] **M44** [Ops][painel-eficiencia] **Detector de intenção (`roteador-agente-trigger.py`) só cobre o Hub por ora** — decisão explícita do Paulo (2026-07-30) de adiar a granularidade "qualquer projeto, qualquer usuário" (o conceito certo é medir os AGENTES, não o Hub). Quando for a hora: propagar o hook via `settings.json` compartilhado (mesmo padrão já usado por `telemetria.py`), e dar visibilidade ao erro do POST de sync em `sincronizar_painel()` (hoje silencioso — não dá pra saber de fora se um sync específico falhou).

## Processo / tooling / eficiência
- [ ] **M3** **"Rebind pass" de tokens** em telas antigas feitas com hex cru (capacidade pronta; rodar quando priorizado).
- [ ] **M7** [mapeador] Registrar no mapa de reuso o **sistema de token por componente** (novo/antigo/misto) — índice que A1/A2 consultam.
- [x] **M13** ✅ Template `Desktop_layout` **publicado** na lib 👁️ (setKey `d37b84ba…`) → páginas compostas por instância + slot.
- [ ] **M16** [A4/A7][avaliar] **Code Connect como capacidade FUTURA (não agente novo).** O Figma Code Connect formaliza o mapeamento código↔Figma (Dev Mode mostra o snippet real; o design→code do A7 ganha suporte de 1ª classe). **Pegadinha decisiva:** o Code Connect mora **dentro do repo de código** e o `<PRODUTO>/` é **READ-ONLY** pra nós → não dá pra commitar lá (precisaria o time de dev adotar). Por isso o nosso **`components.contract.json` já é "o Code Connect de quem não pode escrever no repo"** — mesma ideia, adaptada à restrição, e melhor pro Hub **open-source** (quem clona leva o DS legível por máquina junto). **Decisão:** manter o contrato como fonte; avaliar Code Connect só como **evolução do A4/A7** (gerar/publicar mappings a partir do contrato) se/quando o time de dev topar — **não** criar 12º agente. Já no roadmap do `ds-contract` ("próximo avançado"). (origem: avaliação 2026-07-19) **[2026-07-19] ✅ IMPLEMENTADO como capacidade das skills** (a pesquisa do Medium confirmou a prática): a tensão do read-only foi **desfeita** — as tools `add_code_connect_map`/`get_code_connect_map` do Figma MCP guardam o mapa **no lado do Figma** (`source` = referência textual pro componente), **não** geram `.figma.tsx` no repo. A4 projeta no refresh (Camada 2 da skill `mapa-do-design-system`); A1/A7 consultam antes de re-inferir. Escrito nas skills; falta só o A4 executar num refresh ao vivo (requer sessão Figma). **[2026-07-20] Caso Intercom (vídeo Sneak Peek) VALIDA forte:** "a real potência é o Code Connect — dá ao Claude o caminho real pro componente, então ele **importa do código em vez de recriar**"; e o Dev Mode mostra o selo "Connected → button.tsx". **Prioridade concreta:** *executar* o Code Connect nos 3 🟢 (Button/ButtonIcon/Select) e ver o "Connected" no nosso Dev Mode — tirar do escrito pro provado. Reforçar na skill do A7 o "importa via o caminho do map, NÃO recria". **[2026-07-20, tentativa de execução — nada escrito no Figma] BLOQUEIO técnico achado (read-only):** `add_code_connect_map` exige o **nodeId EXATO do component set publicado**, e (a) os `refNode` do contrato apontam pra **PÁGINAS** (ex.: Select `1:1377` = canvas "Select - New", não o set), (b) o metadata do Button `1313:4897` estourou o limite de tokens, (c) a lib tem **várias versões** de "Button"/"Select" (search: 5 sets "Button") sem **ponte direta key↔nodeId**. Escrever com nodeId impreciso poluiria o Dev Mode do **DS oficial de produção** → parei. **Caminho seguro (= o próprio fluxo da Intercom):** o designer **seleciona o set no Figma e copia o link da seleção** (traz o node-id exato) → mapeia aquele 1 como piloto → confirma "Connected" → depois os demais. **Drift revelado:** os `refNode` do contrato precisam virar nodeIds de component set confirmados (tarefa do A4 — atualizar `components.contract.json`). **[2026-07-20] RAIZ = BLOQUEIO DE PLANO (não técnico):** o piloto com o link da seleção do Button (node-id 1313:8028, endereço exato — o fluxo Intercom resolveu a ambiguidade) esbarrou em `get_code_connect_map` → erro "You need a Dev or Full seat on an **Organization or Enterprise plan**". `whoami`: time `Produto - <EMPRESA>` = tier **pro**. **Logo, Code Connect nativo do Figma é INDISPONÍVEL no plano atual (nem leitura).** A Camada 2 (projeção/selo "Connected") fica **pendente de upgrade pra Enterprise** — decisão de negócio/custo, não técnica. **Mas o valor central já é nosso via `components.contract.json`** (A1/A7 reusam o componente real em vez de recriar), independente de plano — é a alavanca de agora; o selo no Dev Mode é o "a mais" que o Enterprise destravaria. Skills A4/A1/A7 já ajustadas com essa ressalva.

## Arquitetura do Hub como plataforma (lente de "system design")
> Origem (2026-07-19): leitura dos 40 white papers de system design/arquitetura (artigo javarevisited) por 6 leitores em paralelo, traduzindo cada princípio atemporal → oportunidade pro Hub. Filtrados por transferência real (Hub operado por designers, não SREs) e pela regra "só somar, nada de ruído". **Fio condutor: M18 (telemetria) destrava M19; o resto é barato e sobretudo blinda decisões que o Hub já tomou bem.** Ordem sugerida: M18 → (medir 2–3 semanas) → M19; M21/M22/M24 em paralelo (baratos).
- [ ] **M18** [Ops/observabilidade] **Telemetria de execução — o gap nº1 (12/12 agentes em Opus, zero medição).** Trace log append-only (JSONL) onde cada spawn de agente escreve 1 span: `{trace_id, agente, modelo, tokens_in/out, custo_estimado, duração, status}`, via hook `Stop`/`SessionEnd` (mesma cultura do `hub-autosync` no SessionStart). Sempre-ligado (volume de time de design não pede amostragem); `trace_id` por tarefa correlaciona cadeias (ex.: 3→2); A6 vira o rollup semanal. **Bônus (SWIM): saúde de conexão** — quando um conector MCP (Figma/Notion/Calendar) falha 2× seguidas (suspeita, não pânico na 1ª), escrever 1 linha em `estado-atual.md` (`Notion: suspeito desde …`) pro próximo agente ler antes de tentar. Origem: Dapper (tracing) + Tail at Scale (medir p95, não média) + SWIM. **P–M · ruído baixo.** Serve direto o objetivo "facilmente monitorados". **[2026-07-19] ✅ INFRA PRONTA (aprovada pelo Paulo):** `.claude/hooks/telemetria.py` grava span por `SubagentStop` (custo/tokens/duração por `agent_type`) e por `SessionEnd` (loop principal) em `memoria/telemetria/traces.jsonl`; registrado em `settings.local.json` (async, à prova de falha); tabela de preços dos 4 modelos embutida; gitignored (dado local append-only, evita conflito). Testado contra transcript real (estimou ~$30 numa sessão longa) + entradas inválidas (nunca quebra, exit 0). Verificado ao vivo que subagentes têm transcript separado → SessionEnd×SubagentStop não duplicam. Vale a partir da próxima sessão. **Falta:** acumular 2–3 semanas de dados; A6 virar o rollup semanal (parte "saúde de conexão"/SWIM ainda não feita). **[2026-07-20] 🐛 BUG DE ATRIBUIÇÃO achado ao construir o painel (dogfooding):** os 5 spans desta sessão saíram todos como `agent="general-purpose"` / `claude-opus-4-8` / ~26M tokens cada (total irreal $118), quando os subagentes eram **Sonnet**. Causa provável: o `transcript_path` do `SubagentStop` aponta pro **transcript da SESSÃO PRINCIPAL** (opus, contexto 1M com cache_read gigante), não pro do subagente → o hook agrega o principal 1× por SubagentStop (quíntupla contagem + modelo/custo errados). **Fix a investigar:** confirmar o que `SubagentStop.transcript_path` realmente aponta; se for o principal, usar `agent_id` pra isolar o sidechain do subagente, ou filtrar por `isSidechain`/`parentUuid`, ou medir só no `SessionEnd` com desagregação por agent_id. **Até o fix, os números de custo/token por agente NÃO são confiáveis** (a estrutura/coleta funciona; a atribuição não). **P–M · ruído baixo.** **[2026-07-20] ✅ BUG CORRIGIDO.** Raiz confirmada: `transcript_path` do `SubagentStop` = o do LOOP PRINCIPAL. Fix no `.claude/hooks/telemetria.py`: localiza o transcript real do subagente em `<session_dir>/tasks/<agent_id>.output` via glob direcionado (session+agent) → modelo/tokens CORRETOS (Sonnet, não Opus); se não achar → span `measured:false` (sem número falso); + dedup contra re-disparo; + guard pra ignorar payload vazio. Testado isolado (Sonnet $0.48 vs $90 falso). traces.jsonl zerado (61 spans do bug → backup no scratchpad). Dados reais a partir da próxima sessão → destrava M19 e alimenta o artefato com número de verdade. **Falta ainda:** A6 virar o rollup semanal + saúde de conexão (SWIM).
- [ ] **M19** [Ops/eficiência] **Tiering de modelo por agente — SÓ depois de M18 (medir antes de rebaixar).** Right-size: Opus onde há julgamento/voz/fidelidade (A1, A2, A3, A7, A11); Sonnet/Haiku para leitura/enumeração/sumarização/roteamento (A4 em checagem, A5, A6, parte do A11). A troca é só o campo `model:` no front-matter da def (Agent tool já aceita override). Métrica-guia = "tarefas resolvidas por token/custo" (o "queries-per-joule" do FAWN). Origem: FAWN + Mesos (scheduling em 2 camadas) + WSC (custo por operação). **P · ruído baixo-médio** (médio só se pular a medição — risco que o time já reconheceu em 2026-07-19). Alinha com a decisão já registrada de proteger A11/A1/A3/A7 em Opus.
- [ ] **M20** [Ops/DS] **Versão/geração + "offsets" no ds-contract (detecção estrutural de staleness).** Campo `generation`/`updated_at`+hash no topo de `tokens.dtcg.json` e `components.contract.json`; cada consumidor (A1/A2/A9/A10) registra a geração usada no seu output/`estado-atual.md`; a próxima sessão compara ("trabalhei na ger. 4, atual é 6 → vale rodar A4"). "Offset": o agente avisa "o mapa/contrato mudou desde sua última leitura" em vez de trabalhar com dado obsoleto em silêncio. Sem lock nem infra — número gerado pelo próprio commit. Origem: Raft (term/index monotônico) + Kafka (offset por consumidor) + GFS/Dynamo (read-repair). **P · ruído baixo.**
- [ ] **M21** [Ops/federação] **Sinal legível quando o auto-pull NÃO consegue fast-forward (a "partição" do CAP).** Hoje o `hub-autosync` silencia quando diverge/árvore suja — correto em não forçar merge, mas o designer fica sem saber que tem commits locais fora do núcleo. Adicionar, no branch "divergiu" (reaproveitando a lógica de `git` já existente), um aviso passivo legível: "sua cópia divergiu do main / tem N commits locais não sincronizados — abra PR ou chame o time". Origem: CAP (tornar a janela de convergência visível) + Dynamo (eventual consistency com reconciliação delegada). **P · ruído baixo.** Serve "fácil distribuição entre times".
- [ ] **M22** [Ops/contexto] **Compactação/poda do log de aprendizados (`aprendizados.md` já em ~379 linhas, crescendo sem poda).** No passo 3 do loop de auto-aprendizado (CLAUDE.md), ao retroalimentar a skill, marcar a entrada como `[compactado → skill X]`; trimestralmente arquivar as compactadas em `aprendizados-arquivo-AAAA-TX.md`. + verificador leve (Python, sob demanda/refresh): confere se cada "Retroalimentado em X" aponta pra arquivo/seção real e se a tag `[A#]` bate com um agente existente (pega drift log↔view, tipo README "6 habilidades" quando já eram 11). Origem: LSM-Tree/GFS (log → materialized view + compaction) + Kafka/CORFU (view derivada do log). **P · ruído baixo.** Serve "janela de contexto precisa". **[2026-07-19] ✅ FEITO (aprovado):** regra `[✓ compactado → …]` adicionada ao passo 3 do loop (CLAUDE.md) + auditor `scripts/aprendizados-doctor.py` (read-only) que conta linhas/entradas/compactadas, valida tags `[A1..A12]/[Ops]` e checa se cada "Retroalimentado em `caminho`" existe. Rodado: diário em 384 linhas/52 entradas/0 compactadas; **achado real** = 6 entradas antigas apontam p/ skills com nomes pré-rename (`estudio/`, `regras/`, `ouvinte/`, `replicador/`) — histórico, não reescrever. Compactação começa daqui pra frente; arquivar quando passar de 450 linhas.
- [ ] **M23** [Ops] **Manifesto declarativo do Hub + drift reflexivo (o Drift Score apontado pra dentro).** `hub-manifest` curto (ao lado do `components.contract.json`) declarando os 12 agentes esperados + par skill↔def + ferramentas mínimas (ex.: A3 read-only). Script barato pendurado no `hub-autosync` (SessionStart) compara declarado × real e **só relata** drift (skill órfã sem def; agente que deveria ser read-only e não é; agente de catálogo desatualizado). Sem ação automática — só visibilidade. Absorve a ideia do `OWNERS` (path→agente dono, consultivo). Origem: Borg/Omega/Kubernetes (estado desejado declarativo + reconciliation loop que só reconcilia/reporta). **M · ruído baixo.**
- [ ] **M24** [Ops/federação] **Changelog por skill/def, povoado pelo passo 3 do auto-aprendizado.** Seção `## Changelog` curta no rodapé de cada uma das 12 skills/defs núcleo: cada retroalimentação (já obrigatória) gera 1 linha (data · o que mudou · é breaking pra quem consome?). Times que fazem auto-pull passam a **ver** a evolução em vez de herdá-la às cegas. Origem: Borg→Omega→Kubernetes / GFS→Colossus (evolução incremental sob compatibilidade, sem "flag day"). **P · ruído baixo** (é texto, não muda comportamento). **[2026-07-19] ✅ FEITO (aprovado — Paulo pediu que virasse também a vitrine de adoção):** `## Changelog` semeado nas 12 defs (`.claude/agents/*.md`, uma linha inicial honesta, sem inventar datas retroativas); passo 3 do loop (CLAUDE.md) agora povoa o changelog quando o comportamento de um agente muda; **vitrine `scripts/agentes.py`** lê núcleo + catálogo + pessoais (`~/.claude/agents/`) e mostra por agente: slug · modelo · gatilho · última mudança, com o comando de adoção (`adotar-agente.sh`) pros do catálogo — a "loja de agentes" que o Paulo descreveu (ver o que mudou + instalar dali). Testada (12 núcleo em ordem A1–A12; catálogo/pessoais vazios). **Evolução possível:** versão HTML/artifact da vitrine se o time quiser algo visual pra navegar.
- [ ] **M25** [Ops/eficiência] **Receita reusável de "map-reduce" (fan-out) documentada — o "Hive" do Hub.** Uma referência curta formalizando o padrão que já rodamos ad hoc (23 extratores do contrato em paralelo + merge determinístico; auditoria de 17 agentes): (1) lista de itens, (2) prompt por item, (3) fan-out via Agent tool paralelo, (4) merge determinístico validado (JSON válido, contagem bate). O designer pede "roda pros ~110 componentes que faltam" e a receita já existe. Útil sobretudo pra A4 (cauda longa do contrato) e A10 (documentar em lote). Origem: MapReduce + Hive (camada declarativa sobre fan-out). **P (só doc) · ruído baixo.**
- **Descartados por honestidade (sem transferência útil / overengineering pra escala atual):** hedged/tied requests do *Tail at Scale* (disparar réplicas concorrentes — desnecessário p/ ~12 agentes); orquestrador event-driven completo estilo *SEDA* (fica só na versão enxuta: teto de spawns Opus concorrentes + regra de fallback nas skills); motor de propagação estilo *Pregel* (só se o contrato passar a modelar dependências "X usa Y" entre componentes); *Zebra GC* e *TreadMarks* (Git + hook já resolvem sincronização/poda melhor); front-matter estruturado no catálogo estilo *Consul* — **revisitar** só quando a federação passar de ~5–6 agentes contribuídos (hoje a prosa de desambiguação do CLAUDE.md resolve).

## Workflows de product design com IA (lente do Medium)
> Origem (2026-07-19): 5 leitores em paralelo destilaram ~14 artigos do Medium (2025-26) sobre workflow de product design com IA (Nurkhon caiu em paywall). **Achado-cabeçalho: o Hub já está no estado da arte ou à frente** — skills=playbook versionado, DS legível por máquina + drift, memória entre sessões, gate humano, papéis por etapa (12 > os "5 agentes" dos artigos), sem orquestrador central (validado por 2/3 fontes), as-is/rotular proposta = tudo já é prática recomendada. Oportunidades = poucas e cirúrgicas; várias FECHAM CICLO com o que já temos.
> **[2026-07-19] 3 implementadas (Paulo pediu):** (a) **racional de decisão no A2** — Regra de ouro 6 na skill `estudio-de-design` (proposta anexa 2-3 linhas de "por que essa direção"); (b) **Code Connect (=M16)** — skill `mapa-do-design-system` projeta o map no Figma no refresh (Camada 2, resolve o read-only), `codigo-ao-figma`/`figma-ao-codigo` consultam antes de re-inferir; (c) **síntese de pesquisa (M26)** — skill nova `sintese-de-pesquisa` (experimento, não agente). Changelog das 4 defs afetadas atualizado (A1/A2/A4/A7). Nada commitado ainda.
- [ ] **M26** [etapa nova/A2·A5] **Síntese de pesquisa + "voz dos dados" — a etapa descoberta (citada por 4 dos 5 leitores).** Duas faces: (a) sintetizar entrevistas/testes de usabilidade (transcrever→clusterizar→afinidade→**rascunho pro designer validar**, nunca publicar sozinho) — o A5 já faz esse loop pra comentários do Figma e as skills genéricas existem (`summarize-interview`, `affinity-diagram`, `usability-test-plan`); (b) **grounding**: cruzar quantitativo (Amplitude/Databricks) + qualitativo (Kustomer/comentários) pra **checar se a proposta bate com o comportamento real** — NÃO pra "gerar insight" sozinho. **Começar pequeno** (skill isolada ou extensão do A2/A5), medir uso, **só promover a agente numerado (A13) se o uso justificar** — evita inchar o núcleo. Origem: convergência dos 5 leitores + briefing (Amplitude/Databricks subusados). **M (skill) / G (agente) · ruído médio** (o risco é pular o checkpoint humano — o limite central dos artigos). **[2026-07-19] ✅ FEITO como SKILL** (não agente): `.claude/skills/sintese-de-pesquisa/SKILL.md` — 4 amarras (humano no loop; grounding≠gerar insight; IA não formula a pergunta; PII/emoção = freio), playbook (Amplitude/Notion/Kustomer/Databricks → skills de research existentes → rascunho pra validar). Já aparece na barra `/`. **Falta:** medir uso → promover a A13 só se recorrente.
- [ ] **M27** [A4/DS/CI] **DS Drift Score como check automático de PR.** O ingrediente difícil já existe (drift score do A4 + `components.contract.json` + baseline versionado); falta só o **wiring de CI**: um step que roda o drift a cada PR que toca `Ath*`/`Eds*` e posta o score como **comentário informativo** (vira gate bloqueante só depois que o time confiar no sinal). Origem: leitor "governança de DS" (agentes que checam padrões em CI/PR) — prática madura recomendada. **M · ruído baixo** (automatiza algo que já fazemos à mão).
- [ ] **M28** [A6/Ops] **Poda por telemetria — A6 reporta agentes/skills com ZERO uso.** Fecha o ciclo com o M18: o relatório semanal (A6) lê `traces.jsonl` e lista "capacidades sem uso nas últimas N semanas" como sinal de revisão (visibilidade, **não** remoção automática). É a poda simétrica à adoção — segura o acúmulo silencioso (o "ruído" que a regra de ouro combate). Origem: leitor "workflow diário" (curadoria implacável de ferramentas). **P · ruído baixo** (só um relatório a mais sobre dado que já existe).
- [ ] **M30** [A2·A7·A8/skills] **Encadeamento explícito: "pare e pergunte se faltar decisão de produto" + confirmação entre etapas.** Adicionar 1 frase de checklist nas skills que criam/encadeiam: se a intenção/decisão de produto não estiver clara, **pare e pergunte — não invente** (embrião já existe no A7); e ao encadear (A3→A2, A1/A4→A7), apresente o output do agente anterior e confirme antes de prosseguir (evita fan-out silencioso). Origem: leitor "multi-agente" (antipadrão da "reinterpretação silenciosa" do Todd Orr). **P · ruído baixo** (reforça regra existente).
- [ ] **M31** [A2/avaliar] **Fase 0 de rascunho descartável no A2** (baixa prioridade). Opção explícita de gerar 2-3 direções rápidas (Figma Make, MCP já conectado) **antes** de instanciar componentes reais — divergência barata; a entrega final continua reuse-first (regra 6). **Fronteira dura:** rascunho nunca vaza pro Figma "de produção" sem virar componente real (senão quebra reuse-first). Origem: leitores "workflow diário" + "governança". **M · ruído médio** — só se o A2 sair "certo mas sem graça".
- [ ] **M32** [A2/referência] **Checklist de UX para features de IA no produto <EMPRESA>.** `.claude/references/agentic-ux-patterns.md` (Goal→Plan→Action→Review visível · mostrar confiança · gates de aprovação p/ ação de alto risco · undo sempre) que o A2 consulta **quando a proposta é uma feature de IA** (ex.: conciliação/categorização automática). Origem: leitor "research/limites" (designing agentic experiences). **P/M · ruído baixo** (só ativa no pedido certo).
- [ ] **M33** [A9/A7] **Design playground em código (aprendizado do caso Intercom).** A Intercom dá a cada designer um "design playground" — uma seção no código-base pra prototipar **em código** usando os componentes REAIS do DS (via Code Connect), com deploy experimental → link + PR (o engenheiro aproveita ~80% do código). Nós somos **read-only no `<PRODUTO>/`**, então o playground **não pode ser lá** — mas o **`../design-system/` (Storybook standalone do A9) já roda os componentes reais** (alias Vite `@<produto>`) e já tem deploy (Vercel/Chromatic) + CI. Oportunidade: estender esse projeto como o **nosso playground** — o A7 constrói protótipos de **tela** lá (não só stories de componente), no fluxo "copia link do Figma → Claude importa os componentes reais via Code Connect → protótipo idêntico → deploy experimental + PR". Contorna o read-only elegantemente (é o mesmo espírito, adaptado à nossa restrição). **Diferença do vibe-coding descartado:** aqui usa-se o DS REAL (não UI genérica de v0/Bolt), então NÃO é o atalho que gera drift. Origem: vídeo Intercom/Sneak Peek (Domingo, Staff PD). **G · ruído médio** (investimento — validar demanda antes de construir).
- **Onde estamos À FRENTE da Intercom:** no vídeo (23:24) ele **admite não ter resolvido** a paridade Figma↔código automatizada ("existe uma lacuna no mercado pra uma ferramenta onde o que está na tela é código real, não screenshot"). Isso é *exatamente* o nosso **DS Drift Score + contrato legível por máquina** — temos o embrião do que falta pra eles. Não copiar; reconhecer e evoluir (posicionamento).
- **Confirma o M16 (Code Connect):** a pesquisa reforçou ligar Figma↔código por **mapeamento semântico explícito** (Code Connect), não inferência visual — o A4 geraria/atualizaria o map no refresh e A1/A7 consultariam em vez de re-inferir. Já está no backlog como M16; a pesquisa valida priorizá-lo.
- **Descartados conscientemente (o lado cético — vale tanto quanto):** vibe-coding solto de MVP (contradiz o contrato read-only → drift); persona bots / synthetic users / heatmaps preditivos (capability sem uso/dado); orquestrador central (já descartado, reconfirmado 2/3 fontes); assistentes de reunião tipo Fireflies (fora do escopo DS+Code Ops; A8 já lê o Notion estruturado); cerimônia completa de specs/policies do Todd Orr (overengineering p/ 1 designer). **Onde a IA NÃO deve entrar:** formular a pergunta de pesquisa (contexto de negócio humano); ler sarcasmo/emoção; decidir citar usuário identificável — se algum dia tocar PII do Kustomer, síntese final + citações exigem revisão humana antes de sair do agente.

## Rotina
- **Refresh semanal do mapeador** (rotina cloud) mantém o mapa de reuso fresco + drift check.

- [ ] **M75** [Ops/painel][pendente-acesso] **Conectar métricas de NEGÓCIO da empresa ao painel de eficiência (impacto real).** Na Tab 3 do painel, o bloco "não medimos" foi removido (2026-07-20); o item **impacto no produto/negócio final** o Paulo quer endereçar, mas depende dele passar o **acesso às métricas da empresa** (fonte a confirmar — soou "chancoon"; provável ChartMogul/Amplitude/Databricks). Quando tiver acesso: ligar ao dashboard de forma **medida/honesta** (não especulativo). **Lembrar o Paulo** ao retomar o painel. Memória persistente: `dashboard-business-metrics`. *(era **M35** — renumerado em 2026-08-01 porque o ID estava duplicado; o outro item com esse número é citado em outros docs e ficou com ele.)*
- [ ] **M42** [Ops/painel][V2-proposta] **Hub interativo no painel — chat + terminal + os 13 agentes + Figma→código, dentro do `painel-eficiencia`, não só no VS Code.** Análise dupla (engenheiro de software sênior + designer de produto) feita e registrada em `reports/2026-07-26-proposta-hub-interativo-v2.md` (+ apresentação em Artifact) — **não construído ainda**, é registro estratégico pro Paulo decidir quando avançar. Pontos-chave: orquestrar via **Claude Agent SDK** reusando `.claude/agents`/`.claude/skills` (nunca duplicar); MCPs hoje são OAuth pessoal do Paulo (não escalam pra time sem repensar auth); **terminal cru é o maior risco** (execução arbitrária) → vira log de atividade estruturado; Kustomer/Databricks (PII) precisam de gateway com escopo por papel + auditoria antes de qualquer designer além do Paulo acessar; fluxos novos que faltam desenhar: gestão de agentes (criar/editar/**arquivar**, nunca deletar), gestão de conexões MCP (a aba já existe, hoje é foto parada), caixa de aprovações. Fases recomendadas: V2.0 protótipo single-tenant (só o Paulo) → V2.1 abertura pro time (identidade individual + orçamento de tokens) → V2.2 isolar dado sensível (gateway por papel + sandbox de execução). **G · ruído baixo agora** (é só o registro; vira G real quando entrar em construção — próxima tarefa separada).
- [ ] **M36** [A4/DS] **Ícone "sign-out"/logout ausente no swatch curado de ícones do Figma DS.** Ao montar o botão de sair do template de login (`templates/tela-de-login/`), confirmado (metadata completa do frame "Icons", node `4479:7014`) que os ~130 ícones curados (11 categorias) **não têm** nenhum ícone de logout/sign-out/porta-saída. Usei o `sign-out` direto do pacote público `@phosphor-icons/core` (mesma biblioteca-fonte declarada no Figma: "Utilizamos a biblioteca: Phosphor Icons no peso Regular") como exceção clara e documentada — não é do swatch oficial ainda. **Ação sugerida pro curador (Agente 4):** adicionar `sign-out` (e talvez `door-open`) ao frame de ícones oficial, categoria "Ações & Controles de UI" ou nova categoria "Conta & Sessão". **P/pequeno** (curadoria simples, 1 ícone).

- [~] **M63** [DS/decisão-do-Paulo] (aplicado no artefato · pendente no código) **Badge/tag perde a FORMA na linha sob o mouse — decisão de design pendente.** Achado da auditoria de cruzamento (2026-07-31, Seção N.3 achado 7 de `color-audit-material-design.md`): todos os fundos pastel de badge dão **1,00 a 1,22:1** contra as superfícies que os hospedam; o pior é `Info Subtle` #e9f3fe sobre `Background/Hover` #f2f2f2 = **1,00:1** — a pílula desaparece por completo, sobra o texto solto, justamente quando a pessoa passa o mouse pra interagir. **Não é falha WCAG** (o texto do badge carrega o sentido e passa contraste) — é perda de affordance. Opções levantadas na aba "Cruzamentos" do artefato: (a) contorno permanente na cor da família (`Border/Info` etc.) em todo badge; (b) contorno só quando o badge está sobre linha em hover/selecionada (mais leve, mas condicional). **[2026-07-31] ✅ DECIDIDO pelo Paulo — opção (b), a mais leve:** o contorno da família aparece **só quando a linha que hospeda o badge está em hover ou selecionada**; no estado normal o badge segue limpo, sem contorno (zero mudança visual na maior parte do tempo). Detalhe de implementação que evita deslocamento de layout: a borda existe **sempre**, em `transparent`, e só troca de cor no hover — não se adiciona/remove borda. Aplicado no artefato (aba "Cruzamentos", achado 4, com hover real) e os 5 tokens `* Subtle` da paleta viva ganharam a regra na nota. **Falta levar pro código** — entra junto com o M64. **Ponto de implementação mapeado:** `DataTableRow.vue:151-167` (com `:deep()`), porque a pílula pastel é a classe global `.label-status-bg-table` e não o `AthTag` (que tem `pointer-events:none`). Detalhe na Parte 4 de `.claude/references/ds-color-implementation-spec.md`. **[2026-08-02] ✅ APLICADO NO ARTEFATO** (bloco 30 do `override-legado.css`) — e o achado foi do Paulo, olhando a tela: a regra estava decidida desde 31/07, escrita na spec com o ponto de código exato, e **nunca tinha sido aplicada**. Medido em hover antes do fix: "Receita garantida" a **1,00:1** contra a linha e "Matriculado" a 1,04:1 — a pílula sumia justamente ao apontar para ela. Depois: contorno em **4,92:1** (verde) e **6,64:1** (roxo). **Implementação com `currentColor` em vez de mapa família→cor:** cada pílula já tem `color` da própria família, então a borda herda o tom certo sozinha — a alternativa era listar as ~60 classes de estado do `_table.scss` e mapear cada uma, o que envelheceria a cada estado novo do produto. Segue **pendente no CÓDIGO** (`DataTableRow.vue:151-167`, com `:deep()`).
- [ ] **M64** [DS/código] **Levar `Brand Strong` e `Border/Inverse` (anel de foco em fundo escuro) pro código do <PRODUTO>.** Os 2 papéis novos da Seção N estão especificados e demonstrados no artefato, mas ainda **só existem lá** — o código não tem nem o token nem a regra. (a) **`Brand Strong` #5644ad como TEXTO/ícone de marca sobre fundo claro de marca** — hoje qualquer rótulo roxo dentro de chip/faixa/trilho `Brand Subtle` está em 4,41:1 (reprova). (b) **`Border/Inverse` #f2f2f2 como anel de foco quando a vizinhança é escura/saturada** — hoje o anel `#5644ad` some em 7 fundos reais, o pior sendo **1,00:1** (card de ação sob o mouse + foco por teclado: mesma cor exata). Inclui inverter o halo duplo nesses casos (traço claro por fora). **Confirmado no código (2026-07-31):** `--border-inverse` **já está declarado** em `root.scss:85` (= `#f2f2f2`) e está **morto — zero consumidores em todo o `webclient/src`**; e `--border-focus` (`root.scss:87`) e `--background-brand-hover` (`root.scss:52`) são **a mesma primitiva `$brand-600`**, o que explica o 1,00:1. **Correção de um erro meu:** eu havia escrito que o `AthInputCheckbox` usa `#fff` cru como anel interno — **não usa**; eu inferi isso do meu próprio ARTEFATO em vez do código. No código real (`AthInputCheckbox.vue:129-132`) o foco é `outline:none` + `box-shadow: 0 0 4px 0 var(--ath-color-primary-700)` (#8a76eb), um glow difuso — e o fundo do `:checked` (linha 125) é **#8a76eb, a mesma cor exata**: o anel desaparece dentro do próprio checkbox marcado. Padrão copiado em `TreeRow.vue:265-275` e `TabEnrollments.vue:269-277`. **Spec completa (todos os pontos, com `arquivo:linha` e ordem de execução): `.claude/references/ds-color-implementation-spec.md`.** Atenção: o **M66 é bloqueador** — o reset global `_input.scss:84-91` anula o foco com `!important` antes de qualquer token novo chegar. Casa com o M-A10-2 (anel de foco) e com o achado 5 da rodada 4 (`AthCard` com borda neutra sobre fill saturado). **M · ruído baixo** (troca de valor/regra CSS, sem mudar estrutura de componente).

- [ ] **M65** [DS/código][bug real] **6 anéis de foco QUEBRADOS no código — apontam pra variável que nunca foi declarada, e o fallback é indigo do Tailwind.** Achado do mapeamento de 2026-07-31: `--ath-color-primary-200` **nunca é declarado em lugar nenhum** do repo (as decls legadas existentes são só `-900/-700/-500/-100/-50`, em `root.scss:138-142`), e `--ath-color-primary-rgb` **também não**. Um `var()` não resolvido invalida a declaração → `outline-color` cai pra `currentcolor`, e o `box-shadow` renderiza o **fallback cru `rgba(99,102,241,.2)` = `#6366f1`, o indigo do Tailwind** — uma cor totalmente estranha à paleta <EMPRESA>, hoje aparecendo em 5 lugares. Afetados: `EdsButtonIcon.vue:53-57` · `_utilities.scss:334-337` e `:343-347` (`.action-button`) · `HomeCardNavigator.vue:110-114` · `HomeCardEnrollmentGuide.vue:173-177` · `HomeSearchItems.vue:81-86` (este último com `rgba(99,102,241,.2)` **totalmente cru, sem var nenhuma**). **Não é problema de contraste — é anel de foco sem cor definida.** **P/M · ruído baixo** (trocar por `var(--border-focus)`, ou `--border-inverse` onde o fundo for escuro/saturado).
- [ ] **M66** [DS/código][bloqueador] **Resets globais matam o foco antes de qualquer token chegar — `!important` em CSS global.** `assets/scss/input/_input.scss:84-91` faz `box-shadow:none !important; outline:0 !important; border:none` no `.form-control` — ou seja, **mata o foco de todo input Bootstrap do produto**, e qualquer token novo de foco precisa vencer esse `!important`. Mesmo padrão localizado em `AthInputSwitch.vue:102-107` (`!important` duplo, remove o foco por completo), `AthInputTextArea.vue:113-117`, `_toast.scss:85-87`, `NavBar.vue:232-236`, `_sidebar.scss:361-365` (foco reusa o hover, sem anel), `DataTableCheckbox.vue:36` (+ nenhum bloco `:focus` no arquivo), `NavSearch.vue:122`, `AthInputRadio.vue:181`, `EmailMessagePreview.vue:107`. **Precisa ser resolvido ANTES do M64** — sem isso o token novo não tem efeito nos campos. **M · ruído médio** (mexe em CSS global; risco de regressão visual em vários formulários).
- [ ] **M67** [DS/código] **`AthRibbon` variante `.dark`: texto praticamente ilegível (~1,3:1).** `AthRibbon.vue:53-55` define `.dark { background-color: var(--ath-color-secondary-900) }` (#002a3a), mas a classe **não redefine a cor do texto** — o `span` continua com `color: var(--ath-color-secondary-700)` (#2d3849, definido em `AthRibbon.vue:47`). Azul-escuro sobre azul-escuro. Fix: `.dark` passa a usar `Text/Inverse`. Achado colateral do mapeamento do M64. **P · ruído baixo.**

- [x] **M68** ✅ **RESOLVIDO 2026-08-01** [DS/token] **`#005c1f` faz dois trabalhos e não tem nome de papel na paleta.** Achado ao construir a aba **"Papéis de Cor"** (2026-08-01, mapeamento no molde do Material Design 3): o verde `#005c1f` é usado (a) como **hover do preenchimento** de sucesso (`.btn.success:hover`) e (b) como **cor de texto do botão de contorno** de sucesso (`.btn.success-outline`, 7,62:1) — mas **não consta em `getPaletteData()`**, ou seja, não tem nome de papel. É exatamente a mesma classe de lacuna que o **Brand Strong** preencheu no achado 2 da aba Cruzamentos: o valor existe, é reusado em mais de um papel, e como ninguém sabe que ele tem nome, reaparece escrito à mão no próximo componente. **Ação:** nomear (candidatos: `Success Strong-Hover` pro papel de preenchimento e/ou `Text/Success-Strong` pro papel de texto — decidir se é 1 nome ou 2, seguindo a regra "1 hex = 1 nome por categoria de papel" da Seção N.1) e registrar na paleta viva. **P · ruído baixo** (só nomenclatura + 1 entrada na paleta; nenhum hex muda). **[2026-08-01] FECHADO:** virou `Background/SuccessHover` #005c1f, com a nota registrando os 2 papéis (hover do preenchimento + texto do botão de contorno). Junto veio a rodada completa de nomenclatura — ver Seção O de `color-audit-material-design.md`.
- [ ] **M69** [DS/token][do confronto com o M3] **4 papéis do Material Design 3 que não temos — nenhum bloqueia hoje, mas vale a decisão consciente.** Levantados na aba "Papéis de Cor" (tabela "Nós × Material Design 3"): (a) **Secondary e Tertiary** como grupos de acento — o M3 usa pra hierarquia de ênfase (ação principal × secundária × acento contrastante); nós resolvemos com 1 acento de marca + 5 famílias semânticas, o que funciona, mas nos deixa sem um 2º acento neutro pra "ação secundária que não é semântica"; (b) **Shadow** como token — nossas sombras são `rgba` cru no `box-shadow`, sem nome; (c) **Surface Dim/Bright** e os **5 degraus de `surface container`** — temos 3 (Surface · Canvas · Hover), suficiente pro produto atual, mas é a hierarquia de fundo por TOM que o M3 usa em vez de sombra; (d) **Inverse Primary**. **Fora de escopo por ora:** o conjunto **`Fixed`** (4 papéis por acento, cores que não mudam entre tema claro e escuro) só passa a fazer sentido no dia em que existir tema escuro no produto. **P/M · ruído baixo** (é decisão de vocabulário, não de implementação).

- [ ] **M70** [DS/token] **Escala de ELEVAÇÃO (sombra) não existe como token — 4 valores soltos.** Levantado ao fechar o M69: o `Shadow` do Material Design 3 não tem equivalente nosso, mas **não é um token de cor** — a ferramenta usa 4 sombras diferentes por contexto (modal `0 4px 8px rgba(0,0,0,.2), 0 6px 20px rgba(0,0,0,.19)` · popover `0 7px 10px rgba(0,0,0,.16)` · card em hover `0 4px 10px rgba(0,0,0,.15)` · pastilha `0 1px 2px rgba(0,0,0,.08)`). O que falta é uma **escala de elevação nomeada** (ex.: `Elevation/Raised`, `Elevation/Overlay`, `Elevation/Modal`), não um `Shadow` genérico que mentiria sobre haver um valor único. Criar um token de cor só pra "ter o equivalente do M3" seria pior que não ter. **P/M · ruído baixo** (é vocabulário + consolidar 4 valores existentes).
- [x] **M71** ✅ **RESOLVIDO POR DECISÃO 2026-08-01** [DS/token] **Alerta, Risco e Informação não têm preenchimento nem estados — assimetria consciente.** Só Marca, Sucesso e Perigo têm `Background/*` de preenchimento + hover + pressed, porque só eles são botão no produto. As outras 3 famílias existem apenas como badge/texto (contêiner + conteúdo). Está **declarado na matriz** da aba "Papéis de Cor" como célula tracejada "sem token" — a lacuna é visível, não escondida. **Ação:** no dia em que qualquer uma virar botão, criar o conjunto completo de uma vez (`Background/X` + `XHover` + `XPressed` + `XSubtleHover` + `XSubtlePressed` + `Border/XPressed`), seguindo exatamente o molde de Sucesso/Perigo. **Não criar antes** — seriam 15 tokens sem consumidor. **[2026-08-01] O Paulo decidiu criar mesmo assim**, para o sistema ficar simétrico: eu levantei que criar token sem consumidor apodrece a paleta, ele reafirmou, e criei. **9 tokens novos, 6 hex novos:** `Background/Info` · `Background/Warning` · `Background/Risk` (esses 3 **reusam o degrau `Content` da própria família** — mesmo valor, categoria de papel diferente, zero hex novo) + `Hover` e `Pressed` de cada um (6 hex novos, gerados em OKLCH com o mesmo passo das outras 3 famílias: −0,088 de L no hover, −0,094 no clique). Todos nos **dois esquemas** (claro e escuro), 18 pares medidos, todos passam com folga. Consequência: as faixas hachuradas 'sem token' sumiram do mapa e as 6 famílias ficaram simétricas.

- [x] **M72** ✅ **RESOLVIDO 2026-08-01** [DS] **`Content/Warning` e `Content/Risk` são quase indistinguíveis — e o problema é do esquema CLARO, não do escuro.** Ao gerar o tema escuro (2026-08-01), a verificação automática de distinguibilidade entre famílias acusou colisão em Alerta × Risco: distância perceptual OKLab de **0,041 no escuro — e 0,041 também no claro** (abaixo de ~0,05, que é o limiar em que duas cores lidas lado a lado confundem). Ou seja: **o tema escuro não criou o problema, ele revelou um que já existe.** `Content/Warning` #665200 (oliva) e `Content/Risk` #7a4b00 (marrom) têm matizes a só 23° de distância e croma baixo nos dois. Duas famílias que significam coisas diferentes (aviso operacional × risco de crédito) lendo como a mesma cor é falha semântica, não estética. **Opções:** (a) afastar o matiz de uma delas nos DOIS esquemas (mexe em token existente — decisão do Paulo); (b) diferenciar por ícone/forma além da cor (mais robusto e já é boa prática de acessibilidade — cor nunca deveria ser o único portador de significado); (c) aceitar e documentar. Brand × Info tinha o mesmo risco (0,031) e **foi resolvido** no escuro preservando croma (subiu para 0,054). **[2026-08-01] RESOLVIDO — o Paulo mandou consertar.** Diagnóstico da raiz: **não era escolha ruim de cor, é o gamut** — para passar 4,5:1 no branco o L precisa cair para ~0,45-0,57, e nessa faixa **todo matiz entre 50° e 110° perde croma e converge para marrom**. Medido: vermelho a 29° mantém croma 0,200; amarelo a 92° só consegue 0,115. **Fix em duas frentes:** (1) **afastar o matiz** — Alerta 92°→**98°**, Risco 69°→**59°**, gap de 23° para **39°**; (2) **separar por LUMINOSIDADE**, que é a alavanca livre já que o matiz está preso pela semântica da escalada (amarelo→laranja→vermelho): Alerta L 0,48 (`Content/Warning` #665200→**#6F5D00**) e Risco L 0,36 (`Content/Risk` #7a4b00→**#6B2300**). Resultado: separação de **0,041 → 0,153** no texto. **Achado maior no caminho:** os **três pastéis quentes** confundiam entre si (0,028-0,032) — Perigo, Risco e Alerta tinham praticamente o mesmo fundo de etiqueta. Em L 0,95+ nenhum matiz separa; baixando para **L 0,92** o croma vira possível: `#fdfaec`→**#F6E697**, `#feeed9`→**#FFD29E**, `#fdeeee`→**#FFCBC0**, separação **0,053-0,056**. **Bônus: isso ataca o M63** — os pastéis novos sobrevivem à linha em hover (1,12-1,20:1 contra ~1,00 de antes, quando a pílula sumia). Tudo regenerado em OKLCH nos **dois esquemas**, 18 pares medidos, nenhuma reprovação. **Só o lado "Padrão" das abas mudou**; o lado "Hoje" segue com os valores legados, que é a função dele.
- [x] **M76** ✅ **RESOLVIDO 2026-08-01 — 2 ciclos completos aplicados.** [DS/artefato][doc] **Estudo de Cores — feedback de leitor não-designer (rodada 1 de 3: PM + engenheiro).** Sessão de apresentação da aba "Papéis de Cor" para duas personas (PM sem repertório de design · engenheiro do webclient), com o objetivo de reduzir atrito pro próximo leitor. **Achados medidos no Chromium** (não inferidos): (a) **o link compartilhável não funciona** — abrir `index.html#papeis-c4` cai na aba **Princípios**, `scrollY 0`, e o elemento `papeis-c4` **nem existe no DOM** naquele instante, porque os ids são criados por `renderRail()` só para o painel ativo (`index.html:1040-1081`) e **não há nenhum tratamento de `location.hash`**; (b) **os ids da trilha são posicionais** (`id + '-c' + (++seq)`, `index.html:1060`) — hoje `#papeis-c4` é a camada **2**, não a 4; qualquer bloco novo antes desloca todos os links já compartilhados; (c) `veredito()` (`index.html:1445`) está **declarada e nunca usada** — as razões de contraste aparecem sem dizer se passam ou reprovam; (d) **zero menção ao nome da variável CSS** (`--background-brand` etc.: 0 ocorrências no arquivo) — o engenheiro não tem como sair da página e aplicar; o mapeamento existe em `.claude/references/ds-color-implementation-spec.md`, mas o rodapé (`index.html:857`) só cita `color-audit-material-design.md`; (e) **cards de alias ilegíveis** na camada 4 — coluna de 259px com `b{white-space:nowrap}` (`index.html:300,308`) deixa a descrição em **62-78px de largura, 4-5 linhas**, e um caso (`Background/BrandHover`) transborda o card; (f) sem **copiar token** com um clique (as 5 ocorrências de "copiar" no arquivo são do componente `AthCopyText`, não da ferramenta); (g) falta um **resumo de decisão** no topo pro PM (o que é hoje × o que é proposta × o que precisa de aprovação) e uma **legenda do que é um número bom** (o piso 4,5/3,0 só existe em comentário no código). **Rodadas 2 e 3 (outras personas) pendentes — consolidar antes de aplicar.** **M · ruído baixo** (é a casca da ferramenta, não os componentes fiéis).
  - **[2026-08-01] Mesmo exercício estendido às outras 112 páginas** (Princípios · Paleta · Cruzamentos + as 109 de componente, tratadas pelos 2 moldes que elas de fato usam). Tudo medido no DOM renderizado: (h) a aba se chama **"Princípios" no menu e "Padrão" no título** ([index.html:1272](../estudo-de-cores/index.html#L1272)) — e é a **página de entrada** (primeira aba ativa), com 3 frases e nenhuma explicação do que é a ferramenta, de qual é o escopo ou de quando foi auditado; (i) **o catálogo tem 2 moldes e o menu não diferencia** — 69 das 109 abas têm comparação Hoje×Padrão, **40 não têm**: 21 porque o componente herda a cor de outro (`athmulticheckbox`, `athsignerslistinput`…), 11 porque já estão conformes, 1 **não auditada** (`athinputtexteditor`, Quill.js + SCSS externo) e **7 que têm achado real descrito no texto mas sem antes/depois** (`athmodalinformation`, `phoneverificationmodal`, `athprocessingloader`, `edsalertgroup`, `athskeletoninput`, `athmodalheader`, `athfiltercessionstatus`); (j) **`athfiltercessionstatus` é um arquivo de 0 byte no repo** — ocupa uma linha do menu sem ter cor nenhuma; (k) **237 razões de contraste estão escritas à mão** no formato `4.33:1` (ponto) contra 65 no formato `4,79:1` (vírgula, geradas por `razao()`) — ou seja, a regra "nunca escreva um número de contraste à mão" vale só para a aba Papéis de Cor; nas outras 109 o número é texto fixo que envelhece com o hex, e o separador decimal fica inconsistente na mesma ferramenta; (l) **7 dos 70 swatches da Paleta reprovam o próprio rótulo** — `Border/Moderate` **2,85:1** · `Background/RiskVivid` 2,88 · `Border/Warning` 3,01 · `Border/Risk` 3,03 · `Border/Default` 3,36 · `Content/InfoLarge` 3,66 · `Border/Success` 3,75 — porque `swatchTextColor()` ([index.html:1383-1388](../estudo-de-cores/index.html#L1383-L1388)) decide preto/branco por **média de luminância linear com corte arbitrário em 0,62**, e não pelo `contraste()` WCAG que a própria página já tem; pior, a nota do swatch é **11px com `opacity:.78`**, então o valor real é ainda menor que o medido; (m) **a trilha "Nesta página" é genérica em 69 abas** ("Por quê · Comparação · Notas técnicas") e some para 1 item nas 40 curtas, enquanto na Paleta e em Cruzamentos ela é ótima (grupos de cor / os 4 achados) — nas páginas de componente ela não ajuda a pular pro que muda; (n) **os padrões transversais estão invisíveis** — o mesmo achado repetido em N abas nunca é agregado: roxo legado `primary-700` em **24** abas, anel de foco em **19**, borda legada `#e3e4e9` em **13**, hex solto em **17**, e o bypass do overlay em **6 abas com 4 valores diferentes** de escurecimento (`rgba(51,51,51,.5)`, `rgba(0,0,0,.5)`, `rgba(0,0,0,.6)` …); (o) só **7 das 109** abas citam o `arquivo.vue/.scss` de onde a cor sai — o dev não tem por onde começar a aplicar.
  - **[2026-08-01] FECHADO — 2 ciclos de entrevista + aplicação, com 9 personas** (PM · engenheiro · pessoa sem contexto · criança de 5 · criança de 10 · idoso de 80 · especialista de DS · de UI · de Content). Caminho escolhido entre 3 alternativas: **"faca e queijo na mão"** (espinha = linguagem e progressão; as camadas de código e de gestão entraram dentro dele). **Entregue no artefato:** aba de entrada **"Começar aqui"** (o que é · placar contado do DOM · como ler em 4 passos · régua de contraste · 3 estados · ajustes de leitura) · **marcador de situação nas 109 abas do menu** + filtros + contagem por grupo · aba nova **"Problemas que se repetem"** (6 lotes, listas varridas do DOM em runtime, ordem de execução numerada, bloqueio do reset de foco explícito) · **linha do código em cada uma das 70 cores** (`--background-brand` + situação: já existe / trocar valor / declarar) e nas 4 faixas de cada família · **tabela de migração de 35 linhas** (nome antigo → nome novo → o que fazer), levantada lendo `root.scss` e `_educ_colors.scss` · **régua + veredito em todo número de contraste** (`marcarRazoes()`, usa a `veredito()` que estava morta) · **Modo fácil** (esconde hex/medidas/tabelas, explica cada página em 1 frase por situação, glosa cada token em português) · **Texto maior** (só a casca) · **glossário fixo** · **"Veja com os seus olhos"** (3 exemplos reais) · **link compartilhável** (`#aba--secao`, id semântico, apelido pros ids antigos) · **botão de copiar** (variável e link) · **voltar ao topo**. **Correções de contraste na própria ferramenta:** `swatchTextColor()` passou a decidir pela função WCAG (eram 7 rótulos reprovando, o pior 2,85:1); `--ink-faint` corrigido nos 3 blocos de tema (3,18:1 → 5,3:1 no claro, 3,75:1 → 5,9:1 no escuro — consertou 8 elementos de uma vez); criado `--on-accent` porque no tema escuro texto branco sobre `--accent` dava **2,53:1**; `.palette-swatch__note` saiu de 11px com `opacity:.78` para 12,5px sem opacidade; cards de alias empilhados (a descrição vivia em 62-78px de largura). **QA final:** 0 erro de console, 0 estouro, 0 scroll horizontal, **0 reprovação de contraste na casca nos dois temas**, `qa.js` limpo. Detalhe do que a casca v2 passa a exigir de quem editar: seção nova no `estudo-de-cores/README.md`.

- [x] **M77** ✅ **FEITO 2026-08-02** [DS/artefato] **`sistema-de-cores/` — a documentação FINAL do sistema de cores, isolada do estudo.** Pedido do Paulo: trazer ao padrão os componentes que não tinham antes-e-depois (herdando cor, já conformes, não auditados) e publicar um artefato novo, minimalista, que mostre **só o presente** — paleta e papéis definitivos, sem "antes era". **Entregue:** projeto `sistema-de-cores/` com 112 páginas (Fundamentos · Paleta · Papéis de cor · Regras de uso · 108 componentes), **gerado** por pipeline (`gerar/`: extrai do estudo só a coluna do padrão, monta com casca própria) — o lado "Hoje" nunca é lido. **85 valores legados trocados** pelos do padrão (o campeão: `#1a1a1a` em 27 componentes, onde o padrão é `Content/Primary` #002a3a; mais `#7d8097`, `#2d3849`, `#665200`, `#7a4b00`, `#e3e4e9`, `#a6a6a6`, `#f7c9c9`, `#333333`, `#5b5f66`, `#f2f0fc`, `#eef0f4`, `#8a8e94` e os 4 escurecimentos de modal → `Overlay/Scrim`). **Critério de aceite novo e verificável:** o `qa.js` falha se aparecer qualquer hex que não seja um dos 70 tokens — hoje fecha em **zero**, no DOM e no CSS. Também: 70 frases de uso escritas à mão (`gerar/uso.py`), 10 regras de uso, contraste sempre medido em runtime, 0 erro de console, 0 overflow, 0 reprovação de contraste na casca nos 2 esquemas. `AthFilterCessionStatus` ficou fora do catálogo (arquivo vazio no repo). Fronteira registrada nos READMEs dos dois projetos: estudo = investigação · sistema = resultado.

- [ ] **M73** [DS/produto][proposta grande] **Tema escuro: existe como PROPOSTA medida, falta decisão de produto e implementação.** Gerado em 2026-08-01: os **61 tokens** ganharam par claro/escuro na paleta viva do artefato. Método (não é escolha no olho): conversão para **OKLCH** (onde o L é perceptualmente uniforme, mesmo princípio do HCT do Material 3), matiz da família **preservado** entre os esquemas, e o tom **resolvido pelo alvo de contraste** — mira 5,2:1 para texto e 3,4:1 para borda, com folga sobre os pisos de 4,5/3,0 (a 1ª geração parava exatamente em 4,50 e foi refeita: zero folga quebra no arredondamento de render). Âncora da superfície escura = matiz do `Background/Inverse` #002a3a, que **já é usado de verdade** no tooltip e no ribbon — o escuro não nasceu do nada. Verificados 15 pares críticos, todos passam. **A aba Papéis de Cor tem interruptor claro/escuro** que mostra a inversão ao vivo (no escuro o preenchimento vira o tom claro e o conteúdo em cima vira o escuro). **O que falta:** (1) decisão de produto se haverá tema escuro; (2) as famílias Info/Warning/Risk seguem sem preenchimento nos dois esquemas (M71); (3) os tokens `Fixed` do M3 (cor que não muda entre temas) só fazem sentido depois que o escuro for real; (4) implementação no código. **G · ruído baixo hoje** (é proposta registrada, não trabalho em curso).
  - **[2026-08-02] 4 valores do escuro corrigidos + 8 tokens novos ganharam par escuro gerado.** O `verificar-pares.py` mostrou que as tintas do escuro foram resolvidas contra a superfície PADRÃO, quando no escuro o pior caso é a superfície mais CLARA (`Background/Hover`) — 4 tokens quebravam na linha em hover. Ver **M81**; os pares dos 8 novos, em **M80**. A proposta segue proposta: nada disso está em produção.

- [ ] **M78** [DS/produto][dívida com endereço] **12 dívidas de COR POR PAPEL no webclient — cada uma com arquivo:linha.** Levantadas pela auditoria de papel de 2026-08-02 (a que pegou o falso negativo do M79). Não são "cor fora da paleta": são **token certo no papel errado**, que é o que passa batido em qualquer verificação de pertencimento. Hoje estão corrigidas **no artefato** por override (`sistema-de-cores/app/src/estilo/override-legado.css`, blocos 12-22, cada bloco com o motivo e a medida); o que falta é aplicar **no produto**. Em ordem de gravidade de leitura:
  1. **`_table.scss:53-61` (molde repetido ~90×) — etiqueta de estado com texto no tom Vivid.** `color: var(--ath-color-success)` = #2ECC71 sobre #EAFAF1 = **1,95:1**. A variável serve fundo, texto e pontinho ao mesmo tempo; o tom sólido (#007A29) atende os três. Vale igual para info (3,26:1), danger (3,36:1), warning e risk (2,05:1). **É a dívida de maior alcance do lote.**
  2. **`TimelineRepasse.vue:130,140,148` — passo pendente ilegível duas vezes:** ícone branco dentro de círculo #CCCCCC (**1,61:1**) e rótulo #CCCCCC sobre branco (**1,61:1**). A trilha e o círculo que carrega ícone não podem ser o mesmo token.
  3. **`AthRibbon.vue:47,54` — a variante `.dark` troca o fundo e esquece a tinta:** sobra o cinza de texto da regra base sobre #002A3A = **2,31:1**.
  4. **`_colors.scss:23` — `.ath-color--secondary-400` pinta TEXTO com #8C8C8C**, que é `Border/Default`: **3,36:1** (medido no AthFormTitleList, onde é um título). Classe utilitária, então o efeito é amplo.
  5. **Home, `.container-error` — botão terciário roxo dentro de cartão de perigo:** #6B55D8 sobre #FFCBC0 = **3,72:1**. O roxo passa sobre branco (5,24:1); o que falha é dentro do pastel. Ação dentro de superfície pastel deveria herdar a família dela.
  6. **`EnrollmentFunnelCard.vue:189` — `border: 1px solid var(--background-info-subtle)`:** token de fundo desenhando contorno, e a 1,10:1 a borda **não existe** visualmente. Componente novo, já falando a língua dos tokens, e ainda assim pegou a categoria errada.
  7. **`_utilities.scss:283-288` (`.plan-zerodefault`) e `:141` (`.ath-border-secondary`) e `:320-326` (`.action-button`)** — os três usam a escala de FUNDO (ou o tom do texto) para desenhar borda. Nos dois últimos, borda e texto saem da **mesma variável** `--ath-color-secondary-900`.
  8. **`_sidebar.scss:57` — `$color-icon: var(--ath-color-light)`:** o ícone do menu é pintado com #F8F8F8, que é `Background/Canvas`, o fundo das páginas. Ícone é tinta: `--content-on-fill`.
  9. **`AthSwipeMonths.vue:225` — `border-bottom` com `--ath-color-light`** (fundo como borda; 1,04:1, invisível).
  10. **`NavBar.vue:244` — contorno do chip de usuário com a variável do texto dele.** Deu origem ao token `Border/Strong`.
  11. **`AthProgressBar.vue:51` — trilha com `--ath-color-secondary-300`** (#CCCCCC = `Border/Subtle`): preenchimento de faixa de 16px com token de contorno. Deu origem ao `Background/Track`.
  12. **`:focus-visible` não existe para campos** — cada navegador desenha o seu (medido: #005FCC, o azul do sistema, no combobox do EdsInputSelect). Já registrado no bloco 11 para menu/top bar; o lote agora é geral.
  **G · ruído baixo** (nada disso bloqueia o artefato — está resolvido lá; é trabalho de produto, e a lista já vem com o endereço e a medida de cada caso).

- [x] **M79** ✅ **FEITO 2026-08-02 (ver M82)** [DS/processo] **Régua de auditoria de cor: adotar as 3 perguntas em vez de 1, e um só critério para todos os eixos.** O falso negativo de 2026-08-02 ("0 cor fora da paleta" numa tela que o Paulo mostrou errada) não veio de bug: veio de a régua perguntar só *"o hex está na paleta?"*. Ficou: (1) valor existe? (2) **categoria do token bate com o papel** (`color`←Content · `background`←Background · `border`←Border, com as duas exceções justificadas: filete ≤8px pode usar Border, e borda igual ao próprio fundo é redundância)? (3) o par tinta×**fundo efetivo** (compondo alpha subindo a árvore) passa 4,5 / 3,0? A regra vive em **um** arquivo (`sistema-de-cores/app/lib-papel.cjs`) e **um** auditor (`auditar-papel-total.cjs`) roda os 4 eixos (repouso · interação · 108 componentes · estados abertos) — antes eram 5 auditores com 5 réguas, e o resultado que valia era o do mais frouxo. **Levar isso para o `ds-contract`/DS Drift Score**, que hoje também mede pertencimento. Complemento obrigatório: **medir se a tela está cheia antes de declarar cobertura** — 9 colunas vazias em 4 páginas escondiam 2 achados de cor, porque auditoria só vê o que renderiza. **M · ruído baixo.**

- [x] **M80** ✅ **FEITO 2026-08-02** [DS/token] **Os 8 tokens novos ganharam par escuro GERADO, e a verificação de pares revelou 4 dívidas no tema escuro original.** Dívida que eu mesmo criei: ao criar os 8 tokens da auditoria de papel, escolhi o valor escuro **por estimativa** — enquanto o artefato afirma que todo par claro/escuro é "gerado em OKLCH com o mesmo alvo de contraste". Estimativa virou afirmação falsa na documentação, e medindo apareceu o erro: `Border/Strong` escuro a **2,81:1** contra a superfície, abaixo do piso de 3,0. **Feito:** `lib_cor.py` (sRGB↔OKLab↔OKLCH + contraste WCAG + distância perceptual, sem dependência; ida-e-volta exata conferida em 3 hex) e `gerar-dark-novos.py`, que resolve o L pelo alvo do papel **preservando o matiz** — 5,2:1 para tinta, 3,4:1 para contorno, e para os decorativos o **passo de L espelhado**. Os 4 `Content/On*Subtle` foram resolvidos **contra o pastel escuro da própria família**, não contra o card: tinta de família mira o fundo dela. Distinguibilidade entre as 5 famílias no escuro: pior par 0,080 (limiar 0,05) ✓. **Três erros meus que ficam registrados porque são fáceis de repetir:** (a) **direção** — para separar uma tinta CLARA de um fundo, clareia-se; escurecendo, `Border/Subtle` foi para #000000; (b) **piso por papel** — divisor decorativo (`Border/Subtle`, `Border/Faint`) nunca precisou de 3:1 (mede 1,61:1 no claro, por desenho), exigir isso é inventar requisito; (c) **ordem de dependência** — resolvi `Background/Neutral` contra a trilha e a trilha mudou depois no mesmo script, deixando o par a 1,54:1 sem aviso; o Neutral passou a ter **restrição dupla** (separar da trilha ≥3,0 **e** sustentar o `Content/OnFill` em cima ≥4,5). Também: imprimi ✓ sem verificar o obtido — o script agora falha em voz alta quando não alcança. **Novo `verificar-pares.py`:** mede TODOS os pares que as notas da paleta afirmam, nos **dois** esquemas — a paleta não é uma lista de cores, é um conjunto de pares, e nota com número é promessa verificável. Fecha em **0 falhas**. **P · ruído baixo.**

- [x] **M81** ✅ **FEITO 2026-08-02** [DS/tema escuro] **No tema escuro a superfície padrão NÃO é o pior caso — e 4 tokens quebravam na linha em hover.** Achado do `verificar-pares.py`. No esquema **claro**, resolver uma tinta contra o branco é automaticamente o pior caso: todas as outras superfícies (#F8F8F8, #F2F2F2) são mais escuras, então quem passa no branco passa em todas. No **escuro isso se inverte** e a intuição falha: o pior caso é a superfície mais **clara** — `Background/Hover` #2B3539, a linha sob o mouse. As tintas do escuro (M73) foram resolvidas contra `Background/Surface` #1B2429, com folga pequena, e quebravam: `Content/Tertiary` 5,26:1 no card → **4,19:1 na linha em hover** ✗ · `Border/Default` 3,41 → **2,71** ✗ · `Border/Moderate` 2,81 → **2,24** ✗ · `Border/Strong` 3,40 → **2,71** ✗. Ou seja: no tema escuro, a linha em hover é exatamente onde o texto discreto e o contorno de campo somem — estado comum, não borda de caso. **Corrigido cirurgicamente** (`corrigir-dark-pior-caso.py`): só os 4 que falham foram reresolvidos contra o pior caso, com 6% de folga sobre o piso; quem já passava não foi tocado. `Content/Tertiary` #8F969A→**#99A1A5** · `Border/Default` #6E767A→**#7A8286** · `Border/Moderate` #60696D→**#798286** · `Border/Strong` #6E7582→**#7A818E**. **Regra que fica:** tinta é resolvida contra a superfície MAIS CLARA em que ela pode aparecer, não contra a superfície padrão. **Não afeta produção** — o tema escuro é proposta (M73). **P · ruído baixo.**

- [x] **M82** ✅ **FEITO 2026-08-02** [DS/agentes] **O contrato legível por máquina ganhou a camada de PAPÉIS e o Drift Score ganhou o sinal D6.** Fecha o M79. O `ds-contract` tinha só **primitivos** (`tokens.dtcg.json`: escalas `brand.500`, `neutral.450`…, extraídas do código) — com primitivo só se responde "esse hex existe no sistema?", que é a pergunta fraca, a mesma que deixou passar a etiqueta a 1,95:1. **Feito:** (1) **`tokens-semantic.dtcg.json`** (nova camada 0b, gerada de `paleta.json` por `gerar-dtcg-semantico.py`) — os 78 papéis, cada um declarando `<produto>.propriedadesPermitidas` (em que propriedade CSS pode entrar), `<produto>.fundoPadrao` (contra o que é medido — tinta de família mira o pastel dela, não o card), `<produto>.contrasteMinimo` (`null` quando é decorativo por desenho), `<produto>.contrasteMedido` nos dois esquemas e `<produto>.situacaoNoCodigo`; (2) **D6 — papel errado** no DS Drift Score, e **como portão, não como média**: papel errado é defeito, não deriva gradual, então D6 > 0 veta a tela qualquer que seja o score (com as duas exceções: traço ≤8px e borda igual ao próprio fundo); (3) dois "drifts conhecidos" **desatualizados** foram corrigidos — o `content-warning-bold #CCA300` agora tem nome (`Content/OnWarningSubtle` #544500) e o `danger #EB5757 × #E50000` se resolveu com **dois papéis** em vez de um valor (`Content/Danger` #E50000 sobre branco · `Content/OnDangerSubtle` #B70000 sobre o pastel, onde #E50000 dá só 3,36:1); (4) aviso no README para não contar como deriva da tela o que é backlog de DS (`<produto>.situacaoNoCodigo` = `declarar`/`trocar valor`). **M · ruído baixo.**

- [x] **M83** ✅ **FEITO 2026-08-02** [DS/handoff] **Parte 7 da spec de implementação: o lote de papel, pronto para o dev.** Sem isso os 8 tokens novos existiriam só no artefato. Escrita em `.claude/references/ds-color-implementation-spec.md` (467 → 603 linhas), com ordem de execução própria: **7.3** alterar `Content/OnFill` #F2F2F2→#FFFFFF (PR de 1 linha; alinha o padrão a 64 usos que já existem no código) → **7.2** declarar os 8 tokens (PR sem consumo, fácil de revisar) → **7.1** as 5 variáveis do tema legado em `themes/main.scss:22-41`, com a tabela de valor antes→depois e o contraste de cada família (é o mesmo passe do `#2ecc71` que a Parte 6.5 já pedia, agora com o destino resolvido para as 5 famílias, e com a mesma exigência de varredura de regressão) → **7.4** os 10 pontos de papel errado com `arquivo:linha` e medida → **7.5** `:focus-visible` dos campos (depende da Parte 0). Inclui o que **não** é decisão do dev: o botão terciário dentro de superfície pastel (3,72:1) precisa herdar a família da superfície, o que muda comportamento de componente. **P · ruído baixo.**

- [x] **M84** ✅ **FEITO 2026-08-02** [DS/artefato][catálogo] **Catálogo de componentes: 69 de 108 renderizavam pobre — corrigido, com verificador para não voltar.** Pedido do Paulo, com prints: componente montando como casca (o `AthDropButton` como retângulo de 20×16 com uma setinha), campo de busca sem a lupa, `NavSelectTenant` dizendo "Nenhuma rede encontrada", wizard como três bolinhas sem rótulo. **Cor certa, forma errada** — a auditoria de contraste não mede isso. **Feito:** (1) **`auditar-render.cjs`**, auditoria de FORMA (não renderizou · só contorno · campo sem valor · esticado · oco · estado vazio do produto); (2) props e slots preenchidos em **35 componentes**, cada valor lido do `defineProps`/`<slot name>` do arquivo real; (3) **"vitrine"** no palco com a largura que o componente tem no produto (campo 360 · meio 560 · cheia 100%, default por grupo) e palco alinhado ao início em vez de centralizado — um campo não tem 766px no app; (4) suporte a **slots nomeados** no palco (metade do catálogo se compõe por dentro); (5) `userStore` semeado também na página de componente, extraído para `mock/semear-user.ts` (era só na página cheia, e é o que fazia o seletor de rede dizer que não havia redes); (6) chave **`formaEsperada`** no registry, com o motivo escrito, para os 8 cujo desenho é sem texto (esqueleto, `<hr>`, barra de progresso, sobreposição, editor) — declarar em vez de silenciar no auditor. **Fecha em 0/108.** **Efeito colateral que confirma a regra "auditoria só vê o que renderiza":** com a tela cheia, a auditoria de cor (que estava em 0) achou **6 elementos novos**, todos corrigidos — blocos 23-29 do `override-legado.css`, entre eles o hex cru `#2b2256` do cartão selecionado (com o `//TODO: background-brand-pressed do novo DS` escrito pelo próprio dev ao lado, `AthCard.vue:93-94`), o laranja solto `#CF6700` do segmento ativo (`SegmentControl.vue:146`), o rótulo do passo pendente do wizard a 1,61:1, o campo só-leitura com a cor de borda no texto (`_input.scss:23`) e a casca da ferramenta pintando o `<h3>` de dentro do palco. **P · ruído baixo.**

- [x] **M85** ✅ **FEITO 2026-08-02** [DS/processo] **`verificar-props.py`: prop inventada não passa mais em silêncio.** Na correção do M84 eu inventei o nome do campo em **5 de 8 objetos** (`{title}` onde o tipo é `{step, label}`, `{label}` onde é `{text, value}`, `{msgError}` onde é `message`, `{date, description}` onde é `{eventDate, text, details}`, `{text}` onde é `{label}`). O Vue **ignora prop desconhecida sem erro**: o componente monta vazio, com sintoma idêntico ao de não ter props — cheguei a consertar o mesmo componente duas vezes. O script lê o `defineProps` do arquivo real, acusa prop inexistente e, quando a prop é lista tipada (`PropType<X[]>` / `Array as () => X[]`), localiza a `interface X` no repositório e compara **chaves** e **campos obrigatórios**. Achou **28 problemas na primeira execução**, e a maioria **não era da rodada** — `label` onde o repo usa `textLabel` (7 componentes), `subtitle` onde é `subTitle`, `type` onde o EdsButton usa `variant`, `value` onde o AthBadge usa `text`: props inventadas em sessões anteriores, renderizando errado havia semanas sem nenhum sinal. Fecha em **0**. **Rodar junto com o build**, como o `qa-componentes`. **P · ruído baixo.**

- [x] **M86** ✅ **FEITO 2026-08-02** [DS/processo] **`auditar-regras.cjs`: a terceira camada de auditoria — as REGRAS DE USO.** Achado do Paulo olhando uma tela: a **regra 3** do sistema ("etiqueta ganha contorno quando a linha está sob o mouse") estava decidida desde 31/07, documentada na Parte 4 da spec com `arquivo:linha`, e **nunca aplicada**. **Nenhuma das auditorias existentes pegava** — e não por bug: o auditor de papel pergunta "esse valor existe / está na categoria certa / passa contraste?", e ali **não havia valor errado nenhum**; havia uma regra não implementada. Camadas agora: (1) `lib-papel.cjs` → tokens (valor · papel · contraste); (2) `auditar-render.cjs` → forma; (3) **`auditar-regras.cjs` → as regras**. Cobre as 5 das 10 regras que são verificáveis por medição: R2 anel de foco claro sobre vizinhança escura · R3 contorno da etiqueta no hover/seleção da linha · R5 escurecimento único atrás de modal (+ nada de texto sobre o scrim) · R6 borda acompanha o fundo no clique · R7 texto de marca sobre fundo de marca usa o degrau próprio. (As regras 1, 4 e 9 já eram medidas pelo auditor de papel; a 8 é de nomenclatura e a 10 é de escopo — não se observam na tela.) **Achou 7 violações em 3 regras**, todas corrigidas (blocos 30-32): a R3 nas 3 páginas com tabela; a **R5**, com `rgba(51,51,51,.5)` escrito à mão em **três** modais diferentes (`AthModalCenter.vue:139`, `AthModalInformation.vue:128`, `AthModal.vue:214`) onde o sistema tem um valor único; e a **R7**, com a tarja de cobertura usando o roxo cheio sobre o pastel de marca em vez do `Content/OnBrandSubtle` (`_utilities.scss:284`). Fecha em **0**. **Rodar junto com as outras.** **P · ruído baixo.**

- [x] **M87** ✅ **FEITO 2026-08-02** [DS/artefato] **`AthTabs` (e família) recebe STRING em slot de composição.** O registry passa `slots: {default: 'Dados · Faturas · Contrato'}` e o `AthTabs` renderiza o texto cru em vez das abas — ele espera componentes `<AthTab>` como filhos. Mesma classe de problema em `AthTabsIcon`/`AthTab` quando usados isolados. O palco precisa de um mecanismo de **slot com COMPONENTE** (montar filhos reais, não texto) para os componentes de composição. A auditoria de forma não pega porque há texto na tela. **P · ruído baixo.**

- [ ] **M88** [DS/produto] **`outline-offset: 0px` do `.eds-button` resiste a override — 19 casos.** A cor do anel foi resolvida redeclarando `--border-focus` no elemento (variável vence sem disputar cascata), mas o offset não tem variável e a disputa de cascata contra `.eds-button[data-v-…]:focus-visible` não se resolveu, mesmo com `!important` + especificidade 1,2,0 + injeção da folha por último (ver `foco.css`). Tarefa de produto: `EdsButton.vue:136-140`, trocar `outline-offset: 0px` por 3px. **P · ruído baixo.**

- [ ] **M90** [Ops/Agentes] **Decidir se o MCP Databricks (Genie) vira agente dedicado ("Radar de Dados", proposto) ou fica capacidade do Agente 3.** Novo MCP conectado (2026-08-06): consulta em linguagem natural a base master de dados da <EMPRESA> (métricas, produto, área, dashboard, ganhos da semana). Por ora, plugado só como **"Fonte 3"** do Agente 3 (`regras-de-negocio`) — ver `.claude/skills/regras-de-negocio/SKILL.md`. Paulo já sinalizou que outros agentes vão querer isso (A2 pra PRD, possivelmente A6 pro relatório semanal). **Revisitar quando 2+ agentes precisarem dele com frequência** — aí sim promover a agente próprio (dono da busca no Databricks + distribuição correta do dado pros times), em vez de cada agente reimplementar o mesmo acesso. Detalhe da decisão no chat: `memoria/estado-atual.md` (2026-08-06, continuação 70).

- [x] **M89** ✅ **FEITO 2026-08-02 (pelas rodadas de gatilho/estados; conformidade 108/108)** [DS/artefato] **3 componentes ainda sem medição de token, com causa identificada.** (a) `AthInputTextSearch` — o `modelValue: 'Ana'` está no registry mas o campo renderiza vazio, então não há texto para medir cor (provável `v-model` que exige handler, ou o `Field` do vee-validate precisando de contexto de formulário); (b) `AthPopover` e (c) `AthModalCenter` — pintam cor em elemento TELEPORTADO que a coleta não alcança mesmo com o limite de tamanho baixado para 4px. Nos três, a lista antiga foi preservada pelo guarda-corpo em vez de receber lista inventada. **P · ruído baixo.**
